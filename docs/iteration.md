# 迭代演进文档

> 按时间追加的迭代日志。每个有意义的迭代（新机制、规则修改、关卡批次、pipeline 进展、重要修复）记一条。
> 设计层面的「为什么」在 [design.md](./design.md)，这里记「做了什么、验证结果、遗留问题」。

## 条目模板

```
## #编号 日期 — 标题
- 改了什么：
- 为什么：
- 如何验证：
- 遗留问题：
```

---

## #65 2026-08-28 — 修复浅色偏好进入 LV.999 后的白底样式泄漏

- 改了什么：根据协作者截图复现并定位主题叠加问题：根节点进入彩蛋时同时保留 `data-theme="light"` 与新增的 `data-level-theme="lv999"`，浅色专用的高优先级选择器仍会命中，导致操作区 `.control-button` 与选关面板未完成 `.level-item` 等组件被硬编码白底覆盖。现将全部浅色专用选择器统一改为 `:root[data-theme="light"]:not([data-level-theme="lv999"]) ...`，包括浅色变量根、页面背景、HUD、操作按钮、选关 / 规则弹窗、教程、反馈与完成态；进入 LV.999 时整套浅色覆盖一次性停用，离关移除特殊属性后自动恢复。新增 `style.test.ts` 静态守护，若以后出现未带隔离条件的浅色根选择器会直接失败。
- 为什么：只给截图中的白色按钮和关卡卡片补两条深色覆盖，会遗漏规则弹窗、教程卡或通关反馈中的同类泄漏；主题是互斥的视觉层，应该在选择器入口隔离，而不是依赖每个 LV.999 组件以更高 specificity 反压浅色规则。
- 如何验证：定向 `style.test.ts` 与 chem render 20 项通过；全量 `pnpm test` 14 文件 / 461 项通过；`pnpm build` 与 `git diff --check` 通过。级联复核：浅色 + LV.999 时只剩基础深色规则和 LV.999 覆盖，浅色专用选择器零命中；移除 `data-level-theme` 后浅色规则重新命中。
- 遗留问题：本轮依据截图和选择器级联完成修复，当前环境仍没有浏览器截图工具；请在原复现路径刷新后再看一次操作按钮、67–74 未完成关卡卡片、规则弹窗和最终通关卡，确认没有浏览器缓存旧 CSS。

---

## #64 2026-08-28 — LV.999 赛后彩蛋：零冗余满级关与玩家 / 骇客主题

- 改了什么：① 新增 `level-75.json`，内部稳定 ID 为 `109.5°-999`、玩家可见编号为 `LV.999`、关名 `GAME NOT OVER`；关卡是 66《四段全谱》的 180° 旋转 + 五色一一重映射 NG+ 版，保留四阶段 / 双弹射 / 双光格 / 双空穴 / R 护罩 / 撞核全机制闭环，par 23，并把 `moveLimit` 从原版 par+3 收紧为 par，零冗余。② 壳层用特殊 ID 统一驱动展示：关卡导航写 `LV.999 // HIDDEN RAID`，选关末尾增加 `LV.999 隐藏挑战`章节及霓虹入口，通关卡显示 `GODMODE CLEAR · GAME NOT OVER`；`?game=chem&level=999` 会经既有边界钳制直达最后一关。③ DOM 增加临时 `data-level-theme="lv999"` 覆盖，Canvas 增加第三套 `lv999` 语义调色板；紫黑底、冰蓝 / 玫红霓虹、像素切角、扫描线与轻 glitch 全部由 CSS / Canvas 程序化完成，无图片、依赖或联网运行时。离关即恢复玩家原深 / 浅偏好，特殊关点击主题开关只提示当前主题已锁定，不改写存档；动效支持 reduced-motion。④ 基线表扩到 75 关，特殊 ID 作为文件编号规则的唯一显式例外；增加全机制事件、撞核不可绕开、零冗余预算、特殊元数据与第三调色板渲染守护。
- 为什么：公开角色物料把银狼 LV.999 包装为「无敌玩家 / 我独自满级」，并出现「隐藏关卡已解锁」「第 999 场：GAME NOT OVER」等游戏内元叙事，适合作为不引入故事的新游戏+彩蛋。复用经验证的全机制 capstone 并做同构重编，比截止前再塞一条新规则更稳；零冗余红线让它在操作要求上真正高于原版，而不伪装成仅改皮肤的第 75 关。
- 如何验证：`pnpm solve chem level-75` 得到最短解 23 步（访问 41888 状态）；全量 `pnpm test` 13 文件 / 460 项全过；`pnpm build` 成功；`pnpm audit:chem` 显示 level-75 决策密度 39%、最长连续通勤 6、访问状态 41888；`pnpm typecheck`、定向 334 项测试与 `git diff --check` 也通过。
- 遗留问题：当前环境没有可用浏览器截图工具，紫—蓝—玫红主题仍需在 390px 手机、低矮横屏和浅色偏好进入 / 离开路径各目视一次；23 步零容错是否“难而公平”需真人试玩，若只感到惩罚，应只放宽到 par+1，不改四段结构。角色主题是原创程序化致意，没有使用官方图像，但公开展示 / 比赛提交前仍应确认主办方对第三方 IP 彩蛋的口径。

---

## #63 2026-08-28 — 红线定量修订（两档：零冗余 / +3）与棋盘红色警示动画

- 改了什么：① 预算定量按协作者口径修订为两档：零冗余关 69 / 70 / 71 / 74 保持 `moveLimit = par`（命题本身是「预算使撞核不可替代」），其余预算关（57–66、72、73）从 par+1 统一放宽为 **par+3**（三步容错：判死 ≥+4 绕行、不必一步不错）；72/73 hint 的「只留一步余地」改为「留了三步余地」。② 新增步数警示动画：剩余 ≤3 且未胜时棋盘四周亮红色警示框（`.budget-alarm`，`--tone-red` 描边 + 内外辉光 + 0.45s 脉冲），**每动一次跳一下**（applyDir 成功后重启 pulse，强制回流重启动画；prefers-reduced-motion 降级为静态）；HUD「剩余」变红阈值同步从 0 改为 3；撤销 / 换关 / 胜利时随 updateHud 自动熄灭。③ levels.test.ts 预算断言改为分档校验（零冗余档 = 最短解、宽松档 = 最短解 + 3）。
- 为什么：协作者反馈——红线不需要太紧，统一 +3 合适；但「一步都不能多」的关保留原样（69/70/71/74 的撞核命题依赖零冗余才成立）。警示动画同理：接近耗尽时需要比 HUD 数字更醒目的持续性警告，且每步都要有「正在逼近红线」的反馈。
- 如何验证：`pnpm vitest run src/games/chem/levels.test.ts` 309 项全过（含分档断言与 69/74 预算内撞核约束搜索，零冗余关 moveLimit 未变故不受影响）；`pnpm typecheck && pnpm test && pnpm build` 全过；动画为纯渲染层（DOM/CSS），不触碰引擎与 stateKey。
- 遗留问题：① 警示阈值固定 3（对 par 很短的关如 59（par 9）= 后三分之一才亮，观感待真机确认）；② 零冗余四关在警示框亮起后基本等于「最后三步必须全对」，真人体验待盲测；③ 警示框目前只在 chem 预算关出现，t3 无预算、不涉及。

---

## #62 2026-08-28 — v6 步数预算（moveLimit）+ 空穴道岔：高难池 67–74

- 改了什么：① 引擎新增可选 `moveLimit`（level.ts schema + ChemState + step 守卫 + stateKey 预算段 + initialState 透传）：moves 达到预算后一切动作无效（撞墙同语义），预算关 stateKey 含剩余步数、无预算关逐字节不变；HUD 增加「剩余」统计（耗尽未胜红色警示）。② 新关 67–74 共 8 张：「转辙」族——67 北口回声（波反弹换向 + 三段接力，构建中扩成三段防 6 步短路）、68 唤醒（先西后北由键奇偶锁死）、69 遥扳（红线 + 漏斗墙 + 撞核二跳级联 T→Vn→V2）、74 终章（同一岔心翻三次、第三次只由飞珠完成，(4,1) 堵墙防 Vn 二次进攻替代撞核）；「红线」族——70/71/72/73（61/63/64/65 的镜像 / 180° 旋转变体 + moveLimit=par 或 par+1，hint 注明红线）。③ 57–66 统一补 moveLimit = par + 1，hint 追加「本关步数有红线」。④ levels.test.ts：基线扩到 74 关、预算收紧断言（最短解 ≤ moveLimit ≤ 最短解+1）、69/74「预算内绕开撞击无解」约束搜索、67/68 波向语义轨迹、68 奇偶锁结构性断言；main.ts 选关面板加「转辙与红线 67–74」章节。⑤ 按 #61 术语表套用 67–74 的 hint（清除「飞珠」），术语守护测试范围从 01–56 扩到全部关卡。
- 为什么：协作者三点反馈——弹射撞核总可被「走过去自己撞」替代；建议给高难关加总步数限制；三臂可当道岔控制传导方向、开盾可断链。分析确认：回合制下撞核与步行只差步数不差可行性（弹道线格子全可行走），预算是让撞核不可替代的唯一硬手段；三臂道岔存在真实结构自由度（hole 换边 = 出口换向、波先翻后传、键奇偶锁）。
- 如何验证：engine.test.ts 新增 5 项预算语义测试（44 项全过：达到预算冻结、恰好第 limit 步获胜允许、stateKey 预算段、无效动作不耗预算、solver 预算感知）；levels.test.ts 全过；逐关 `pnpm solve`（67–74 par 依次 11/10/10/14/14/11/19/14）；`pnpm audit:chem` 决策密度 29%–43%；最终 `pnpm typecheck && pnpm test && pnpm build`。
- 遗留问题：① 67–74 难度排序与删留待真人试玩；② 红线 = par 的四关（69/70/71/74）要求零冗余操作，真人是否可接受待盲测，必要时统一放宽到 par+1；③ 镜像 x 会翻转光照顺时针方向感（含三臂的关卡 par 漂移甚至无解，65 的 x 镜像即无解），旋转 180° 才保 par——已记入 design §5 v6 作为造关事实；④ 「开盾断链」命题族与既有回授闸门关（39/40/43）重合度高，本批未单独出题，若盲测反馈需要可再扩展；⑤ 预算关的「卡死未胜」目前只靠 HUD 红色剩余提示，是否需要更醒目的「步数耗尽」反馈卡待盲测定。

---

## #61 2026-08-28 — 玩家文案风格整治（全量）：56 条 hint 与 34 个关卡名重写、规则书、教程卡、守护测试

- 改了什么：#59 试点语感经作者确认后全量铺开（术语定名表与红黄绿牌见 design §10）。① 01–56 全部 hint 逐条重写：删金句 / 对仗 / 公式 / 说教冒号，淘汰术语全部换成定名（点火 / 震动 / 飞珠 / 离去珠 / 余料 / 终投 / 喷流 / 转轴 / 触光 / 预调 / 校准 / 插槽 / 游离 / 构型 / 纯翻转 / 换边 等），「缺口」统一为「空穴」；只改表述不改方位提示，规则陈述句保留（level-39 R 护罩按 #54.6）。② 34 个含淘汰术语 / 造词的关卡名改为「一句口语或一个具体画面」：双态闸门→一条臂，两扇门、回授闸门→撞完自己关门、三响余料→别急着停手、转轴再喷流→先转再弹、三段注入→一颗珠开三道门、光启缺口→开局先踩光 等；「一撞，两座一起转」「碰不到的中心」「空手只有一次」「空穴断路器 / 保险丝」类达标名保留。③ main.ts 规则书 11 条与一句话目标全部重写，清除泄漏进玩家文案的化学词「构型」「游离」，规则 02 标题去「180°」。④ tutorial.ts 清理约 50 处残留（光照格→光格、纯翻转→空手撞、翻转 180°→翻转半圈、离去珠 / 飞珠→弹出的珠、撞核→撞中心、封闭面反馈改写等），tutorial.test.ts 同步 8 处断言。⑤ render.ts Inspect 面板「构型周期 · 进攻翻转」→「翻转前后 · 撞一次翻一次」；选关面板章节标签「共振传导 / 光照与分步 / 结构碰撞与回授闸门 / 综合 mastery」→「共振 / 光格与阶段 / 撞结构与再生护罩 / 综合」。⑥ levels.test.ts 新增守护测试「01–56 hint 不含 37 个淘汰术语」防回潮（同日新增的 57–66 批次不在本轮范围，测试注释已注明入库时套用）。⑦ design §10 两步计划标记落地；AGENTS.md 两处旧关卡名引用同步。
- 为什么：试点确认可行后铺开；关卡名实改 34 个（超出试点估计的约 12 个），因淘汰术语已渗透进名字，不定名会与术语表自相矛盾。
- 如何验证：`pnpm typecheck` / 全量 `pnpm test`（410 项，含 57–66 新批次基线与语义轨迹）/ `pnpm build`；守护测试确认 01–56 hint 无一含 37 个淘汰术语；hint / name 为纯展示字段，不进 `stateKey`，各关最短解基线与机制语义测试不受影响。
- 遗留问题：① 57–66 批次的 hint / 名仍带旧术语（飞珠 / 转轴 / 余料等），入库定稿时按 §10 术语表套用；② 新关卡名与 Inspect「翻转前后」表述是否更易读，待真人盲测；③ 守护测试词表如需扩充（新造词出现时）须同步更新。

---

## #60 2026-08-28 — 高难扩展池 57–66：全机制组合加量（10 关）

- 改了什么：新增 chem 关卡 level-57…level-66（高难候选池，10 关），不改动 01–56。每关一个此前未成关的机制组合命题：57 撞核转喷口（撞核目标首次是弹射中心本体，撞完喷口再瞄准，撞出珠回收作二次发射弹药）、58 空穴准星（撞核翻空穴换边接通共振键，护罩挡住同连锁的追溯）、59 撞核关闸（空穴撞进再生护罩监视槽主动关闸，保护已达标中心；构建中重构过一次布局）、60 双光时序（两块光格 + 双阶段护罩 + 三臂周期 3 的顺序谜题）、61 喷口对射（对脸双喷口撞核再瞄准 + 撞光转轴；修掉光照格乒乓——拾取必再转一次轴，终点按 2 次旋转设计）、62 空手关闸（监视臂怕染料，关闸只能空手撞；双受害者分支 + 燃烧陷阱）、63 三级火箭（弹射三级接力，中段喷料落光格转轴）、64 罩内预瞄（护罩内预转喷口与邻居，解锁即射；用注入槽颜色签名堵死直接取代捷径）、65 合流（三段闭环：共振烧穿开闸 → 一束光同时瞄三件事 → 撞核关闸 → 余料终投）、66 四段全谱（四段闭环 + 双弹射 + 双光格 + 空穴接收端）。同步：levels.test.ts 基线表扩到 66 关、段落测试 37–66、10 关机制事件表、撞核不可绕开约束搜索（57/58/59/61/64/65/66）、59/62 去再生护罩不可解证明、60 双光格各踏一次 + 北闸先南闸、63 三级接力交互轨迹、66 第二级喷料落点锁定；main.ts 选关面板加「高难候选池 57–66」章节；design.md §5 新增决策小节、§6 表格与收尾段更新。
- 为什么：协作者要求在现有 66 关后追加高难关，「把所有机制都用起来尽量多组合出新机制」，数量可以多、后续挑选；并明确难度不能用最短步数衡量、终审排序由真人试玩决定（文件序号只是组合新颖度粗排）。全部组合刻意避开 51–56 已覆盖的六个命题。
- 如何验证：逐关 `pnpm solve chem level-XX`（par 依次 11/11/9/11/14/13/14/11/19/23）+ `pnpm trace:chem` 人工回放核对设计意图；`pnpm vitest run src/games/chem/levels.test.ts` 270 项全过（含全部新约束搜索）；`pnpm audit:chem` 决策密度 31%–55%（62 的 7 步通勤为北角关闸往返、65/66 的 6 步为多段接力站位成本，有意保留）；最终 `pnpm typecheck && pnpm test && pnpm build`。
- 遗留问题：① 57–66 难度排序与删留完全未定，待协作者试玩（可 `?game=chem&level=XX` 直达）；② 60/61/64 的光照格「拾取再转轴」语义在 hint 中只以「踩一次」「落点」暗示，是否需要更明确的教程待盲测；③ 65/66 未做真人盲测，四段闭环的认知负荷是否过高待验证；④ 开发中三次踩到同一类坑并修复——光照格踏入即转轴（61 乒乓）、光照全局转轴会误转撞击目标（64 首版被 17 步无撞核解绕开）、注入槽签名必须区分撞核与同面取代（64 二版），这三条语义陷阱已沉淀进约束测试，后续造关直接复用。

---

## #59 2026-08-28 — 玩家文案风格整治（试点）：去生成腔、术语定名表入档

- 改了什么：design.md §10 新增「游戏内术语表与文案风格」决策（15 概念定名表：每概念只留一个玩家可见词，淘汰 飞珠/离去珠/余料/震动/点火/闸门/游离 等 60+ 别名；红黄绿牌风格规则；试点→全量两步执行计划）。试点落地：① 教程卡英文眉题去设计行话 `CORE INPUT`→`BASICS`、`MECHANIC REVEAL`→`NEW RULE`（tutorial.ts + main.ts 占位符）；② 教程卡 5 处改写：「这就是染色」两处（一处改命名句「放珠、翻转，这一套合起来就叫染色」，预演拍不再重复命名）、「这就是预演：虚线是染色后的构型」→「先别松手：虚线是这一撞之后的样子」（顺带清除泄漏的化学词「构型」）、「关门有时不是障碍，而是在保护…」→「护罩一关…有时候故意关上它，是为了保住已经对齐的中心」、「它不是一次性打开的门…」→「它会反复开合…全看另一条控制臂的状态」；③ 重写最严重 10 条 hint（level-04/05/10/11/25/39/46/49/50/55）：删金句（「手变空本身就是资源」「每颗珠都有自己的去处」）、公式（「染色 = 交换 + 翻转」）、淘汰术语（点火/烧到/离去珠/注入物/终投/余料/三响/副产物/校准/插槽）；level-39 按 #54.6 决策保留 R 护罩规则陈述句；④ tutorial.test.ts 同步 3 处字符串断言。
- 为什么：全量文案审读 + 真人反馈发现两类问题：文案带 AI 生成腔（金句癖、「不是 A 而是 B」对仗、「这就是 X」命名腔、说教冒号、三连排比），术语膨胀（约 15 概念对 60–70 种写法，空穴/缺口同关混用，「构型」「游离」化学词违反 §10 红线）。先试点让作者确认语感，再铺开全量。
- 如何验证：`pnpm typecheck` / 全量 `pnpm test`（357 项全过；hint 为纯展示字段，不进 stateKey，各关 zod / solver 最短解基线不受影响）/ `pnpm build`。
- 遗留问题：全量批改未开始——规则书 11 条、其余 46 条 hint、教程卡其余金句、约 12 个造词关卡名（双态闸门/回授闸门/三响余料等）、Inspect 面板「构型周期」，均待试点语感确认后执行；届时在 levels.test.ts 加「hint 不含淘汰术语」守护测试防回潮。

---

## #58 2026-08-28 — 第五关开场「设置导览」：三拍依次介绍动画 / 教程 / 暗色开关

- 改了什么：`shell/tutorial.ts` 第 5 关（index 4）新增开场三拍（`moves === 0` 时）：①「动画」开关切 2 倍速、②「教程」开关控制引导卡、③「暗色」开关切换主题，kicker 为 `SETTINGS · 01–03 / 03`，每拍 `advanceOnTap` 并通过扩展后的 `controlTarget`（新增 `'animation' | 'tutorial' | 'theme'`，类型导出 `TutorialControlTarget`）让壳层高亮状态栏对应开关；`main.ts` 的 `isTutorialControlAwaiting` 放宽参数类型，`updateTutorial` 对四个控件（提示按钮 + 三个开关）统一管理 `tutorial-focus` 高亮，新增 `withControlBeat` 助手——试用对应开关与轻触棋盘同样完成该拍（先判定再执行动作，与 `requestStepHint` 同序）；`style.css` 增加 `.tutorial-toggle.tutorial-focus` 金色脉冲描边。
- 为什么：设置开关此前没有任何教学入口，玩家（尤其触屏）不易发现动画倍速 / 主题切换；放在第 5 关（核心搬运中段、动作已被前三关吃透）作为轻量 UI 导览不打断节奏，第 6 关起机制教学密度高不适合再插拍。
- 如何验证：`pnpm typecheck` / 全量 `pnpm test`（357 项，新增第五关三拍断言：各拍 controlTarget 与 kicker、第 4 拍恢复物流引导）/ `pnpm build`。
- 遗留问题：待真人盲测确认三拍是否会被无脑连点跳过（试用开关完成该拍的激励是否被注意到）；若反馈冗长可压缩为两拍（教程 + 暗色合并）。

---

## #57 2026-08-28 — 第二关教学分拍：先聚光讲「走到背面」，行走期收起阴影

- 改了什么：`shell/tutorial.ts` 第 2 关（index 1）引导拆为三拍——提示按钮介绍拍之后新增一拍「绕到白箭头的反面」（聚光中心 + `advanceOnTap`，tip 为「点击或轻触棋盘任意位置继续」）；玩家点掉该拍后行走期不再常驻中心聚光（模型无棋盘提示时壳层整体隐藏引导卡，撞墙 / 撞封闭面的反馈仍会弹出）；只有站上箭头反面进攻位才恢复聚光与手势，标题「位置对了，向 X 撞入」并配新的进攻位说明文案。
- 为什么：试玩反馈：原先引导卡从开局到就位一直聚光在中心上，玩家全程在提示阴影底下走位，体验差；应先聚焦讲清目标、点击收起，等真正就位再提示「位置正确」。
- 如何验证：`pnpm typecheck` / 全量 `pnpm test`（356 项，第二关引导测试改为三拍断言：聚光拍 advanceOnTap、行走拍无聚光无手势、就位拍含「位置对了」+ E 向手势）/ `pnpm build`。
- 遗留问题：待真人盲测确认行走期无引导卡是否会迷路（提示按钮 H 仍可用）。

---

## #56 2026-08-28 — 色珠「显脏」定点修复：同相派生描边

- 改了什么：`drawAtom` 在浅色主题下不再用调色板里的藏青描边（`rgba(29,48,62)`，线宽为半径 16% 的粗环）与 0.78 透明度藏青纹样画珠，改为 `shadeColor(填充色, 0.62)` 派生的同相深色环（黄珠配深金环、绿珠配深绿环），纹样降为中性深色 `rgba(43,38,30,0.55)`。深色主题描边 / 纹样零改动。测试护栏同步：断言每色派生描边比填充更深且对棋盘底对比度不低于填充、纹样保持深中性且 alpha 在 0.5–0.68。
- 为什么：真人复测确认其余 UI 已满意，唯独色珠（尤其黄珠）仍显脏——排查确认「脏」不是填充色本身，而是上一轮为可读性加强的深墨藏青粗圈与重纹样：蓝灰环压在暖黄填充上视觉混合成灰褐。同相派生描边既保边缘清晰（对比度反而高于填充），又不引入异色灰圈。
- 如何验证：全量 `pnpm test`（356 项）/ `pnpm typecheck` / `pnpm build`。
- 遗留问题：需真机目视确认派生描边（0.62 系数）与纹样（0.55）的最终观感；不合适只调这两个常数。

---

## #55 2026-08-28 — 浅色「显脏」返工：清亮填充回归 + 深底调组件修复

- 改了什么：
  - **Canvas 调色板回退**：上一轮为追 ≥3:1 把浅色黄 / 绿 / 阶段金 / 光照格 / 暗键灰蓝全部压暗（黄 `#b38200` 等），真人复测反馈「暗暗的、灰灰的、脏脏的」——与 #52 已否决的「褐金压暗」方案同病。现回退为清亮值（黄 `#d99a16`、绿 `#2fa866`、阶段金 `#d39516`、光照格 `#dfa723`、暗键 `#8ba2b4`），保留全部透明度类改进（光照格呼吸下限 0.9、暗键 / 空穴 0.66、休眠 R 罩 0.6、控制线 0.5、未来段目标 0.32、原子轮廓 0.5 / 纹样 0.78）——只抬存在感、不压暗色相。
  - **DOM 侧脏感来源修复**：`--accent` 褐金 `#8a6500` → 金 `#b57c00`（下一关按钮、winbar 文案、当前关卡标记等 28 处随之改善）；主行动按钮（下一关 / 反馈提交）浅色改为亮金渐变面（`#ffd977→#ecb643`）+ 深字，与深色的亮金按钮同语义；已通关三 token 从灰暗绿（`#24784a/#aed2bc`）换清亮翡翠（`#0f9553/#93dcb4/10% wash`），反馈 ok 状态色同步；反馈星星选中态由「深底调 8% 金 wash（白底不可见）+ 淡金边」改为 26% 可见水洗 + 深金边 + 外圈光环，hover / 未选中态同步适配；教程聚光圈脉冲色 token 化（`--spot-dim/--spot-bright`，浅色用深金 `rgba(181,124,0,.75)↔#e8a92c`，修复淡金在白底脉冲峰值消失）；winbar / brief-toggle / brief 徽标 / 警示教程卡 / 换出预报标签的淡金边框加深到可见；`--tone-yellow/green` 与 Canvas 同步回退。
  - **测试护栏改口径**：render.test.ts 浅色断言从「填充 ≥3:1」改为「填充 ≥2.2 防 pastel 下限 + 轮廓 / 纹样必须深墨且 alpha ≥0.45/0.7」，与 design §10「填充色不单独承担正文对比度」红线对齐。
- 为什么：可读性修复不能以变脏为代价；#52 已确立「清亮填色 + 深轮廓兜底」为浅色正确方向，本轮只是把违反该方向的压暗值回退，并把「存在感」改由透明度下限承担。DOM 侧问题多为「为深底调的半透明金在白底上消失」这一类，逐个补浅色覆盖。
- 如何验证：全量 `pnpm test`（356 项）/ `pnpm typecheck` / `pnpm build`；静态核对浅色覆盖区不再有未覆盖的深底调金色水洗（除纯装饰扫描线）。design.md §5 v5 决策 7 已同步改写为修订后口径。
- 遗留问题：需真机复测确认——亮金按钮渐变、翡翠完成态、星星选中光环的实际观感；如仍不满意，只调对应 token（`--accent` / `--complete-*` / 星星覆盖块），不再动 Canvas 玩法色。


---

## #54 2026-08-28 — v5 曲线重构（前 50 关完整游戏 + 趣味前置）与浅色 P0 修复

- 改了什么：
  - **关卡曲线重排（60 → 56 关，01–50 为提交用完整游戏）**：删旧 08 / 09（同质搬运）、旧 10（与新 07 重复的 par-1 共振展示）、旧 15（第三张 par-1 展示链）、旧 40（与 25 同板近重复）；新关《碰不到的中心》入新 09（两座中心进攻位被墙永久封死，共振是唯一的门）；候选池 53 / 55 / 58 / 59 提升为正式 43 / 49 / 48 / 46；池剩 6 关（51–56），旧 60 去「终局」前缀，50 恢复唯一终局命名《终局：三段闭环》。全部文件重编号、`id` 对齐、进度键随之自然重置（提交前可接受）。
  - **共振教学倒挂修复**：旧 06（首次出现共振）承担正式四拍教学（暗键 → 面对臂同色 → 亮键 → 传导）成为新 07，旧 10 的延迟教学块删除；多米诺（旧 11 → 新 08）成为第一次刻意使用；新 06（旧 07 空手不变式）补两拍教程。机制首现节奏变为：取代 03 → 共振 07 → 光照 13 → 空穴 18 → 弹射 24 → 护罩 30 → v4 37，核心搬运从 9 关压到 6 关。
  - **浅色可读性 P0**：浅色玩法色全部校准到对棋盘底 ≥3:1（黄 `#b38200`、绿 `#269a5c`，红 / 蓝 / 紫复检通过）；阶段一金色 `#b57c00`、光照格 `#b57c00` + 呼吸 alpha 下限 0.9；`bond` 加深为 `#5f7889`；暗键 / 空穴圈 / 休眠 R 罩 / 未激活控制线 / 未来段目标按主题提亮（新增 `dimBondAlpha` / `idleShieldAlpha` / `idleLinkAlpha` / `futureGoalAlpha`）；原子描边与纹样加强。style.css 三组硬编码深色珠色（`.stat strong` / `.guide-orbit` / `.forecast-dot` 的 data-tone）收敛为 `--tone-*` token 并提供浅色值。
  - **39（旧 43）hint 补再生护罩规则陈述**；design.md:330 的「40 开罩接空穴」编号漂移修正；playtest.md 代表关 / 深链 / D 组成对更新；main.ts 九章节、tutorial.ts 教学索引、levels.test.ts 基线与语义测试全部按 v5 重写。
- 为什么：真人反馈「开局新手引导不清晰；意识到是益智游戏后没有新机制巩固理解，也没有展现玩法趣味点」；审查确认 03–09 连续七关同一动词、前 50 关以机制教学为主、缺少「这里竟然还可以这样？」的关卡，且共振首现（06）与教学（10）倒挂。比赛要求 ≥50 关，定为「前 50 关完整游戏 + 51+ 赛后自由扩展」。
- 如何验证：全量 `pnpm test`（356 项，含新增 09 关「只许直接进攻中心 0、两座封锁中心仅由共振翻转」结构+回放断言、浅色调色板对比度护栏、双主题渲染代表关更新）；`pnpm typecheck` / `pnpm build` / `pnpm audit:chem` 通过；新 09 关独立 solver 验证（最短解 3 = par，E/E/S 一击三翻）。决策与完整映射表先写入 design.md §5「v5 曲线重构」再动代码。
- 遗留问题：浅色新色值需真机 / HDR 目视（与 #51–#53 遗留同批验收）；真人盲测（playtest.md A–D）仍全部未做，v5 的三个假设——教学前置后节奏是否可感、09 关「唯一的门」能否被自主发现、提升的 4 关在 mastery 段的体验——都待盲测证据；若 51–56 扩展池未来继续扩量，按「放飞自我」定位自由发展，不再受正式曲线约束。

---

## #53 2026-08-27 — 浅色浮层、提示与通关状态完整适配

- 改了什么：补齐 DOM 壳层的浮动组件语义色，新增按钮、遮罩、浮层表面 / 边框 / 阴影、正文反馈和已通关状态 token，并分别提供深浅值；教程 / TIP 卡、折叠后重开按钮、单步预测与提示反馈、Toast、通关大卡片、查看棋盘后的通关小栏、反馈评分区、选关 / 规则弹层全部改为使用这些 token。浅色教程聚光由黑色压暗改为浅灰蓝洗淡，滑动手势与键盘提示也切为浅底蓝墨；选关中的已通关关卡改为淡绿底 / 绿勾，当前关卡继续保持金色，二者同时出现时当前态优先。全局按钮浅色背景也由高优先级覆盖改为基础语义 token，避免主按钮、已选反馈标签和功能按钮被意外刷回普通白色。
- 为什么：上一轮主题工作重点在页面骨架与 Canvas，浮层和完成态仍散落着只适合暗色的硬编码，导致浅色模式下 TIP、通关卡片和已通关关卡像嵌入的黑块；高优先级的浅色按钮兜底还会吞掉组件自身状态。将它们收进同一套语义层可以同时解决当前遗漏，并让后续新增卡片默认继承正确主题。
- 如何验证：静态审计浅色模式下所有卡片 / 弹层 / 完成态选择器，检查当前态与完成态的层叠顺序；执行 `pnpm typecheck`、全量 vitest、`pnpm build` 与 `git diff --check`。
- 遗留问题：仍需在真实手机与 HDR 显示器目视浅色遮罩透明度、通关卡片阴影和淡绿完成态；如果环境光下完成态偏淡，只调整 `--complete-*` token，不再局部加深。

## #52 2026-08-27 — 开局因果演示、有效碰撞反馈与早期共振亮点

- 改了什么：第一关从四次静态对象聚光收敛为一拍循环演示：绿色珠绕中心翻转半圈进入同色目标，下一拍立即开放移动 / 撞入；教程模型新增只读 `orbitDemo`，壳层用程序化轨道与彩色珠播放，不改游戏状态。所有有效直接进攻新增玩家向中心前冲、接触点蓝色冲击环 / 三条短射线和回弹，1× 在 120ms 接触后进入翻转，2× 也保留 80ms 可见接触拍；无效进攻继续使用红色撞面反馈。level-06 改为 6 步“紫珠从 A 换出蓝珠 → 蓝珠进攻 B → B 翻转接通绿键并带动 C”，开场只用一拍预告连锁，第 10 关仍正式教学共振。浅色调色板第二轮从灰蓝 / 褐金压暗方案改为更白的棋盘底、更淡网格 / 墙和更清亮的五种玩法色与阶段盾色，预演遮罩也改为浅色洗淡而非灰压暗。
- 为什么：试玩者在第一关连续点击聚光说明，却没有看到“碰撞—翻转—进目标”的完整动作；成功进攻的玩家与中心缺少接触帧，又让旋转像凭空发生。03–05 虽持续增加换珠规则，但真正能展示多结构联动趣味的共振到第 10 关才出现；把一个受控连锁放到第 6 关，可以在不增加操作语法的前提下提前兑现玩法。上一版浅色为追求填充色自身对比度而整体加深，HDR / 高亮屏上黄绿和盾线容易显脏，应由清亮填色 + 深轮廓 / 纹样共同保证可读性。
- 如何验证：level-06 通过 zod 与 solver，最短解固定为 6，决策密度 50%、最长连续通勤 3；新增语义轨迹断言最后一步玩家只邻接 B，但 B / C 同步变化且通关。教程测试断言第一关只有一拍半圈演示、第六关出现连锁预告且第七关退出常驻引导；渲染时钟测试断言有效进攻 0–119ms 前冲、120ms 同时进入冲击与翻转。执行 `pnpm audit:chem`、定向测试、全量类型 / 测试 / 构建与 diff 检查。
- 遗留问题：程序化轨道、碰撞前冲幅度和浅色新配色仍需真机 / HDR 屏目视；盲测重点记录玩家能否在第一击后说出“整核翻半圈”，以及第 6 关最后一击前是否预测到 C 会跟转。若仍不清楚，优先加强接触音画节拍与共振键点亮过渡，不再恢复多页静态聚光。

## #51 2026-08-27 — chem Canvas 深浅双语义调色板

- 改了什么：将 `chem/render.ts` 中散落的 Canvas 颜色收敛为深 / 浅两套语义调色板，覆盖画布与棋盘底色、网格、墙与纹理、结构键线、主墨色、玩家芯、仪表标签、Inspect / 预演面板与遮罩、标记、锁定勾、中心轮廓、原子描边 / 纹样、光照格、危险反馈、五种玩法色及五档阶段色。浅色玩法色单独加深，代表色相对浅色棋盘底板的对比度约为 4.4–5.6:1；舞台 DOM 边框、扫描线、占位底色与阴影也改为主题变量。现有暗色开关通过 `setChemRenderTheme` 同步 Canvas，系统自动切换和持久化手动切换均立即重绘；切换会清空瞬时动画与单步输入缓冲，但不改引擎状态。
- 为什么：只把页面壳切为浅色、Canvas 保持深色会形成突兀的嵌套黑块；直接反色又会改变红 / 蓝 / 绿 / 黄 / 紫的身份和护罩、目标圈、共振键的信息层级。按语义角色维护两套明确色值，才能让玩法反馈在两种底色上保持同一优先级。
- 如何验证：新增双主题代表机制渲染测试，分别渲染 01 / 10 / 18 / 29 / 33 / 43 / 58 / 60，断言画布底色与玩法红色实际切换且浅色路径不再使用旧深底；静态检查确认 Canvas 绘制代码不再直接赋值散落颜色。执行类型检查、全量 vitest、生产构建与 `git diff --check`。
- 遗留问题：无浏览器截图工具，仍需在真实屏幕目视两套主题，重点观察浅色下黄色目标圈、休眠 R 盾、三臂缺口、预演遮罩与墙的区分；若调整，只改对应语义 token，不再回到局部硬编码。

## #50 2026-08-27 — R 盾开盾停顿与三项紧凑显示开关

- 改了什么：继续校准 level-58 的 1× 因果时间线：控制臂于 540ms 落定并打开 R 盾后，受保护中心与其后续共振链额外等待 180ms，至 720ms 才开始旋转；2× 不增加该停顿。状态栏在教程右侧新增“暗色”开关，首次进入仍按系统主题，手动选择深 / 浅后写入 `lexin-games:color-theme` 并优先恢复；三个显示开关统一缩至 54px，≤600px 竖屏与低矮横屏把整组换到状态栏第二行，移动端再缩至 48–50px。
- 为什么：上一轮虽已把 R 盾释放提前到控制臂落定点，但开盾与下一中心旋转只相隔 50ms，视觉上仍像同时发生，不能稳定读出“先开盾、再传播”。自动主题缺少玩家覆盖入口；第三个开关加入后若继续挤在玩法状态同一行，会在手机与横屏侧栏重新产生横向滚动。
- 如何验证：level-58 渲染时钟测试新增断言：中心 2 在 719ms 仍等待、720ms 才进入旋转；执行类型检查、定向渲染测试与生产构建通过，最终再跑全量测试与 diff 检查。
- 遗留问题：无浏览器截图工具，180ms 停顿体感、浅色开关的明暗对比及 390px / 低矮横屏第二行高度仍需真机目视；若停顿仍不清楚，优先加强开盾完成帧而不是继续无限拉长整回合。

## #49 2026-08-27 — 1× 输入缓冲、R 盾因果时序与自动主题

- 改了什么：修复 1× 动画期间第二次方向输入被直接丢弃的问题：新增通用单槽缓冲，只保留最新下一步，动画结束后自动执行；键盘、方向按钮、按钮键盘激活与 Canvas 滑动统一接入，排队方向以蓝色按键态反馈，撤销 / 重开 / 换关 / 切倍速清空。R 盾时间线从“整条动作结束后统一碎裂”改为读取 `reactiveTo` 控制中心的实际翻转结束点：控制臂落定即生成 / 释放，再生盾释放可先于后续共振中心翻转；阶段盾仍在全连锁后解除。状态栏两个开关删除滑块，改为关闭低亮 / 开启点亮的紧凑按钮，动画关闭=1×、开启=2×，继续复用原 localStorage 键。全部关卡右侧字符 `⌄` 改为 CSS 边框绘制的独立 chevron。页面壳层新增 `prefers-color-scheme: light` 自动浅色变量与主要面板适配，Canvas 棋盘保持经玩法验证的深色调色板。
- 为什么：1× 原实现为保护语义动画而直接拒绝输入，快速连按同键确实只会产生一步，并非玩家操作问题；单槽缓冲既恢复响应感又不积累长队列。level-58 终步中控制中心先把控制臂转回红色，R 盾由此开放并允许共振进入三臂中心，旧画面却让三臂中心带盾转完后才碎，视觉因果与引擎相反。字符箭头受字体基线影响发生下沉；滑块又占用已经拥挤的状态栏。自动主题只改 DOM 壳层可安全完成，Canvas 直接反色会破坏玩法颜色层级。
- 如何验证：新增 2 项单槽缓冲测试，覆盖同键快速重复只排一次、不同方向以最新意图覆盖及显式清空；新增 level-58 最短解终步渲染时钟测试，断言 R 盾在 539ms 仍等待、控制臂于 540ms 落定时开始释放，且此时后续共振动画尚未结束。执行 `pnpm typecheck`、全量 362 项 vitest、`pnpm build` 与 `git diff --check` 全部通过。
- 遗留问题：无浏览器截图工具，浅色系统主题、390px 手机状态栏和 level-58 实际动画仍需真人目视一遍；重点确认浅色壳层中的深色棋盘过渡是否自然、单槽缓冲反馈是否足够可见，以及 R 盾碎裂开始与三臂中心启动之间的 50ms 间隔是否清楚。

## #48 2026-08-27 — 动画倍速语义与提示一步首现教学

- 改了什么：把上一轮状态栏的 `清晰 / 快速` 文案改成玩家熟悉的 `1× / 2×` switch：默认 1×、开启后 2×，保留原有清晰因果时间线与快速重叠时间线，偏好键和内部类型不迁移。第二关开场新增一次棋盘外控件教学，教练卡说明「提示一步」只返回当前最短路线的下一步、不替玩家行动且不限次数，同时给底部按钮加金色脉冲与「这里 · 单步提示」指示；实际点击按钮会展示下一步并完成该教学拍，轻触棋盘也可跳过。≤600px 布局让按钮常显「提示一步」文字，不再只剩无名菱形图标；规则手册同步改为 1× / 2× 说法。
- 为什么：`清晰 / 快速` 更像设置术语，`1× / 2×` 能直接表达这是播放速度且默认档仍完整；手机端隐藏所有控制文字后，提示图标又没有通用语义，玩家可能在卡关前后都不知道 solver 辅助存在。把辅助教学放到第二关可以避开第一关已经较密的五拍核心对象教学。
- 如何验证：教程模型测试新增第二关首拍断言，确认目标为 `hint`、文案明确“不代操作”、可点击推进，下一拍恢复原有白箭头引导；执行 `pnpm typecheck`、全量 359 项 vitest、`pnpm build` 与 `git diff --check` 全部通过。
- 遗留问题：DOM 高亮与手机常显文字仍需在真机观察是否被手指、系统安全区或横屏紧凑布局遮挡；盲测时记录玩家首次卡住后能否自主找到提示按钮，以及是否误以为它会自动执行动作。

## #47 2026-08-27 — 取代因果分镜与清晰 / 快速双节奏

- 改了什么：chem 状态栏新增与教程独立、写入 localStorage 的 `动画 清晰 / 快速` 开关，默认清晰。清晰模式把持珠进攻拆成撞击停顿、开口原珠与手持珠沿两条平行弧线反向交换、整核 180° 翻转、共振逐核落定；动画中的共振键读取当前视觉构型，不提前泄漏最终连通状态。弹射撞核改为源中心翻完后飞珠再出发，抵达远端进攻位后才启动第二条翻转 / 共振链；阶段护罩保留到整次时间线结算后再碎裂。清晰动画未结束时忽略新方向输入，撤销、重开、换关仍可立即打断；快速模式保留原来的短翻转与重叠传播。规则手册同步说明双节奏。
- 为什么：试玩反馈表明，不读文字教程的玩家会把取代看成“中心对面的珠突然变色”。旧实现虽然终态正确，却在 260ms 内直接显示动作后手持并旋转结构，没有证明哪颗珠离开、哪颗珠进入；继续增加文案无法服务主动关闭教程的玩家。按语义事件分镜可以建立物体身份连续性，同时避免把 180° 错拆成两个貌似合法的 90° 状态、与光照转轴混淆。
- 如何验证：新增渲染测试切换两种模式，回放 level-03 首次持珠取代，确认清晰时间线比快速模式多出交换与停顿且两档均可完整渲染；执行 `pnpm typecheck`、全量 358 项 vitest、`pnpm build` 与 `git diff --check` 均通过。
- 遗留问题：程序化测试只能验证时间线、状态隔离与渲染不抛错，不能证明玩家真的追踪到了两颗珠；需用教程关闭 + 清晰动画让新玩家试玩 03–05，在动作后复述“哪颗进入、哪颗换出”，再比较快速模式。弹射中心的离去珠在交换、翻转、出射三段之间是否仍保持身份连续，也需列入 27–29 真人盲测。

## #46 2026-08-26 — 后续新机制统一为逐物教学

- 改了什么：将 04 的换出、10 共振、16 光照、17 分步目标、21 三臂空穴、27 弹射中心、33 阶段护罩、41 飞珠撞核、42 飞珠触光再撞核、43 再生护罩全部改为“对象 → 条件 → 结果 → 操作”的点击分拍教学。聚光不再笼统罩住整座中心，而精确落到开口原珠 / 手持珠、暗键与两侧面对臂、光照格 / 白箭头 / 固定色臂、当前 / 未来目标圈、三角核 / 空穴 / 将换边色臂、弹射核 / 喷口 / 离去珠 / 弹道落点、阶段目标 / 护罩编号、远端进攻位、飞珠触光格与 R 罩控制臂 / 控制线 / 被隔离共振键。每段最后一拍才出现真实方向或恢复自由输入；27 在拾珠后继续引导就位，再解释离去珠与落点。壳层输入拦截改为读取当前 `TutorialModel.advanceOnTap`，删除关卡编号与拍数的第二份判定；教程关闭时所有分拍都不拦截。
- 为什么：单卡总览即使文案准确，仍要求玩家同时在棋盘上寻找多个对象并自行拼接结算顺序；尤其共振“先翻后接通”、护罩“动作结算后开放”和 42“先触光再撞核”不能靠一个大光圈说明。统一教学语法也让后续新增机制有明确模板，不再各写一张长说明卡。
- 如何验证：教程测试扩为 18 项，逐拍断言十段机制的标题、`advanceOnTap`、具体聚光坐标与最终操作方向；弹射段另回放拾珠 → 就位状态，确认离去珠位于开口臂、弹道落点为 `[0,1]`、最后一拍才出现长按。执行 `pnpm typecheck`、全量 357 项 vitest、`pnpm build` 与 `git diff --check` 全部通过。
- 遗留问题：这些分拍刻意偏细，仍需真人盲测删节；优先记录玩家在哪一拍已经能预测结果，若连续两拍都只是在复述同一因果，可删除后一拍，但保留对象级聚光与最终操作拍。

## #45 2026-08-26 — 第三关七拍染色流向教学

- 改了什么：把 03 的染色说明拆为七拍对象绑定序列：先聚光游离紫珠与紫色虚线目标圈，第三拍才开放向右移动拾取；拾取后暂停真实输入，依次聚光玩家右上角的手持紫珠、核内白箭头与翻转后的紫色目标落点；第七拍才开放按住 / 拖住预演，预演时聚光回到完整中心 ghost，松手执行。拾珠前两拍和拾珠后三拍中，点击棋盘或方向输入只推进教学，不改变局面；即使教程关闭时先拾珠，重新开启也会从拾珠后的正确拍数继续。
- 为什么：旧 03 在拾珠后用一张长卡同时解释手持、开口、翻转、落点与长按，玩家仍要自己把文字映射到多个对象。拆成“场上紫珠 → 手中紫珠 → 箭头开口 → 对侧目标”的空间流向，才能在第一次染色前形成可预测模型；开口原珠换出的第二条物流继续留到 04，避免一次建立两个新概念。
- 如何验证：教程测试逐拍断言游离珠、目标圈、手持珠、白箭头和最终落点的实际棋盘坐标，确认第 3 拍才出现拾取方向、拾珠后三拍再次暂停动作、第 7 拍才出现 S 长按与单向放入预报，并回归触屏拖住文案。执行 `pnpm typecheck`、全量 356 项 vitest、`pnpm build` 与 `git diff --check` 全部通过。
- 遗留问题：真人盲测需观察七拍是否过长；若玩家在指到箭头后已经能主动说出紫珠落点，可删去第六拍的重复说明，但不得重新合并成一张多对象长卡。

## #44 2026-08-26 — 第一关五拍指物教学与教程总开关

- 改了什么：把 01 的首屏从一张混合说明卡拆为移动前四拍对象辨认：依次精确聚光玩家白色光环、北侧绿色虚线目标圈、南侧绿色珠与核内白箭头，点击 / 轻触棋盘或方向输入只推进讲解、不改变局面；第五拍才出现当前输入方式对应的 WASD / 方向键或滑动动画并开放移动，到达进攻位后继续说明整体翻转 180°。状态栏末端新增可访问的 `教程 开 / 关` switch，选择写入 localStorage；关闭后统一隐藏 01–05 教练层和后续机制首次揭示，保留本关折叠提示与全部规则手册。窄屏开关压缩为 80px 并固定在状态栏右侧，避免回到底部操作区挤压方向键。
- 为什么：上一版仍把玩家身份、胜利目标与移动输入塞进第一张卡，聚光又罩住整个中心，玩家很难把文字逐一对应到白球、目标圈、待搬色珠和箭头。先逐物建立指代，再开放输入，能把“我是谁 / 去哪里 / 搬什么 / 怎么撞 / 怎么操作”变成稳定顺序；熟练玩家则需要一次关闭所有自动教学的明确出口。
- 如何验证：教程测试改为逐拍断言四个聚光坐标、说明文案、`advanceOnTap` 与第五拍才出现方向提示，并回归到位后的撞入 / 180° 说明；执行 `pnpm typecheck`、全量 356 项 vitest、`pnpm build` 与 `git diff --check` 全部通过。
- 遗留问题：仍需真机确认“点击棋盘继续”的四拍节奏是否过慢，以及状态栏开关在 320px 宽度下是否无需横向拖动即可发现；盲测时记录玩家能否在第五拍前主动指出四个对象。

## #43 2026-08-26 — 首关目标教学与常驻规则手册

- 改了什么：重写 01 的第一屏教练卡与关卡提示，先明确「虚线圈颜色 = 该位置需要的色珠」，到达进攻位后再把「站在箭头反面 → 沿箭头方向撞入 → 中心与全部色臂整体翻转 180°」连成完整因果，并以本关绿色目标说明翻转结果。方向区右侧新增「全部规则」按钮与 G 快捷键；可滚动弹窗按正式曲线顺序收录目标、背面进攻、持珠交换、预演、共振、光照 / 分步、空穴、弹射、阶段护罩、飞珠撞结构与再生护罩，另为显式研发入口保留 t+3 规则页。弹窗支持关闭按钮、背景点击与 Esc 关闭，手机端与紧凑横屏使用单列规则卡和 44px 以上按钮。
- 为什么：旧教学虽然分别出现过“白箭头”“背面”“翻到对侧”，却没有在玩家第一步之前说明胜利目标，也没有把站位、输入方向和 180° 结果组织成可复述的一条规则；后续机制只在首次出现时揭示，玩家忘记后缺少统一复习入口。
- 如何验证：教程纯函数测试新增 / 更新目标文案、箭头反面与 180° 断言；执行 `pnpm typecheck`、全量 356 项 vitest、`pnpm build` 与 `git diff --check` 全部通过。
- 遗留问题：仍需用未接触项目的玩家盲测首屏复述与规则按钮可发现性；重点观察玩家能否在第一击前指出目标圈、进攻站位与翻转结果，以及是否把“箭头反面”误解为站在箭头尖端一侧。

## #42 2026-08-26 — 通关反馈快捷标签与弹窗回返

- 改了什么：通关反馈新增六个可多选快捷标签（规则没看懂 / 卡在推理 / 操作不顺 / 走路太多 / 太简单 / 很有意思），与评分一并保存在会话并提交；前端载荷使用固定英文 ID、过滤未知值并去重，FastAPI 用 `Literal` 白名单、数量上限和唯一性校验，旧请求缺省为空标签。星标补齐 `min-height` 后固定为 26×26 正方形（紧凑横屏 24×24），标题右侧说明缩为“可选”。查看终局棋盘后的精简通栏把“已通关”摘要改为可点击按钮，可原样恢复大弹窗，不重建反馈 DOM，因此未提交选择不会丢失。
- 为什么：自由文本填写成本高，而结构化标签只需一两次点击即可解释低分来源；原星标受全局按钮 44px 最小高度影响呈竖长形，说明文字也过度解释。查看棋盘原本是单向出口，会让尚未反馈的玩家无法返回。
- 如何验证：前端新增标签白名单 / 未知值过滤 / 去重 / 载荷测试，vitest 总数增至 356；后端在临时依赖环境用 FastAPI TestClient 验证旧载荷得到空标签、合法标签写入 JSONL、未知标签返回 422。最终执行 `pnpm typecheck`、`pnpm test`、`pnpm build` 与 `git diff --check`。
- 遗留问题：快捷标签的措辞与六项数量仍需根据实际样本使用率删选；若某标签长期无人选择，优先删除而非继续扩充。

## #41 2026-08-26 — 移除装饰开关并压缩移动端操作区

- 改了什么：删除顶栏星形装饰按钮、壳层 `Bundle.setDecor`、切换状态与监听器，并移除渲染层 `setChemDecor` 分支，正式棋盘固定使用默认轻装饰。≤600px 时撤销、重开、标记、提示按钮隐藏视觉文字，只保留程序化图标与 44px 以上命中区，同时补齐 `aria-label`；≤380px 重新分配控制区列宽，避免图标按钮被压窄。预演 Canvas 横幅与教程卡文案统一改为“回到原位或按 Esc 取消”，同时覆盖触屏拖拽与键盘操作。
- 为什么：低频装饰开关占据最醒目的右上角但没有玩法价值；窄屏辅助按钮的竖排文字挤压方向区。旧预演文案只写 `Esc`，又把桌面操作错误地呈现给手机玩家。
- 如何验证：残留搜索确认运行时代码已无 `setChemDecor` / `decorBtn` / `#decor`；渲染测试新增预演横幅文本断言，教程测试新增取消文案断言。`pnpm typecheck`、全量 354 项 vitest、`pnpm build` 与 `git diff --check` 通过。
- 遗留问题：需在 320–430px 真机宽度复核纯图标的首次可辨识度；若单个图标仍难懂，优先用首次轻提示或长按说明，不恢复常驻文字。

## #40 2026-08-26 — 收起教程卡仍保留棋盘指引

- 改了什么：教程收起态改为只隐藏大段文字卡与交换预报；聚光、滑动 / 按键动画和方向键高亮继续显示，小型 `TIP` 按钮仍可恢复完整说明。
- 为什么：遮挡问题来自大卡片本体，而聚光与动作动画承担“看哪里、怎么做”的关键教学职责；把它们一起隐藏会让新手教程只剩一个入口标签，失去情境指引。
- 如何验证：CSS 收起选择器现只匹配 `.board-guide-card`，不再匹配 spotlight / gesture / key；`pnpm typecheck`、全量 354 项 vitest、`pnpm build` 与 `git diff --check` 通过。
- 遗留问题：需在真机确认收起后聚光暗幕是否仍保持足够棋盘可读性；若偏暗应只降低暗幕透明度，不移除指引。

## #39 2026-08-26 — 小屏教程收起与滑动预演防闪

- 改了什么：棋盘内教练卡新增 44px 触控关闭按钮；收起后卡片、聚光与手势动画一并隐藏，仅在原卡片边缘保留小型蓝色 `TIP` 按钮，点击可原位恢复，且收起状态跨教程步骤保持。棋盘滑动改为越过 24px 后只锁定方向，快速松手直接执行；沿该方向继续停留 280ms 才注入一步预演，变向重新计时，并补上极快 fling 没有中间 `pointermove` 时的松手方向判定。
- 为什么：窄屏上的完整教练卡会遮住大部分棋盘；旧滑动模型又在每次普通滑动中立即绘制一次压暗 ghost，造成逐步闪烁。把详细教学交给玩家控制、把预演与“拖住”而非“划过阈值”绑定，可同时保留可发现性和连续操作手感。
- 如何验证：新增 3 项纯输入测试，覆盖快速滑动不预演、停留到阈值才预演、斜向意图过滤；`pnpm typecheck`、全量 354 项 vitest、`pnpm build` 与 `git diff --check` 通过。CSS 结构复核确认收起态使用 `display: none` 移除卡片 / 聚光 / 手势，并保留独立可点击的恢复标签。
- 遗留问题：仍需在真实 iPhone SE / 小尺寸 Android 上确认 280ms 停留阈值与拇指变向容差是否自然；若仍误触，只调输入阈值，不改变“快滑执行、拖住预演”的模型。

## #38 2026-08-26 — 收敛 favicon 图形

- 改了什么：将 favicon 从“带节点、内部箭头和渐变的四面体”收敛为深色底上的三臂蓝色骨架 + 金色中心核，移除端点圆、渐变、边框和额外方向箭头。
- 为什么：原图在 16×16 标签页尺寸下信息过密，多个高亮元素互相争夺注意力；保留一个结构关系和一个中心焦点，更符合游戏的极简轻包装。
- 如何验证：运行 `git diff --check`、`pnpm typecheck`、`pnpm test`、`pnpm build`，并确认 Vite 产物包含更新后的 `favicon.svg`。
- 遗留问题：仍只提供 SVG 版本；旧浏览器兼容性需要实际发布环境确认。

## #37 2026-08-26 — 《109.5°》favicon

- 改了什么：新增 `public/favicon.svg`，以深色棋盘背景、四面体骨架、色珠节点和中心方向箭头抽象出游戏标记；在 `index.html` 注册 SVG favicon。
- 为什么：浏览器标签页和收藏夹需要一个不依赖文字、在 16×16 尺寸仍可辨认的轻量品牌标识，并与现有页面的深海军蓝 / 信号蓝 / 金色视觉语法一致。
- 如何验证：运行 `pnpm typecheck`、`pnpm test`、`pnpm build`，并确认 Vite 产物包含 `favicon.svg`。
- 遗留问题：暂不额外生成 PNG / ICO，多数现代浏览器可直接使用 SVG favicon；如需兼容旧浏览器再补静态回退尺寸。

## #30 2026-08-25 — 通关反馈收集器（构建期可选，dev 默认开启）

- 改了什么：
  - 通关卡片（overlay）新增「通关反馈」区：难度 / 趣味各 1–5 星标（radio，支持方向键），两条都选才可提交，POST `/api/feedback`；成功后按「游戏:关卡」记入 `sessionStorage`，重开本关可一键复评。
  - 构建期开关 `VITE_FEEDBACK_ENDPOINT`（design §8）：未设置时反馈完全休眠（不渲染、零请求），比赛默认产物保持纯静态无后端；dev 默认开启并指向 `http://127.0.0.1:8787`；任意环境可用 `?fb=<url>` 查询参数临时覆盖（cloudflared 快速隧道地址每次重启都变，免重建调试）。
  - 新增 `src/shell/feedback.ts`（端点解析 / 载荷构建 / 评分校验抽成纯函数，配 `feedback.test.ts`）、`server/app.py`（单文件 FastAPI：CORS 全开、`POST /api/feedback` 追加写 JSONL、`GET /health`、`GET /api/feedback` 计数）、`server/requirements.txt`、`server/README.md`（含 cloudflared 用法）；`server/data/` 与 `__pycache__` 进 .gitignore。
- 为什么：正式提交仍是纯静态零联网，但开发期与公网试玩需要低成本收集主观难度/趣味数据来支撑「删关 / 改曲线」决策；评分与 `moves`/`par` 一起上报，便于按实际步数聚类分析难度感知偏差。
- 如何验证：`pnpm typecheck` / `pnpm test`（新增 10 项）/ `pnpm build`；dev 下通关出现评分面板，提交后 `server/data/feedback.jsonl` 落一行；`curl -X POST http://127.0.0.1:8787/api/feedback -d '{"game":"chem","level":1,"levelId":"level-01","difficulty":2,"fun":4,"moves":4}' -H 'Content-Type: application/json'` 返回 `{"ok":true,...}`。
- 遗留问题：
  - 记录是追加式的（同关卡可多次提交，视为更新样本）；如需按玩家去重可后续加 `sessionId` 维度。
  - 快速隧道地址每次重启变化，正式收集前要么用新地址重建前端，要么改用固定命名隧道。
  - 评分 UI 目前只挂在通关卡片；winbar（查看棋盘后的精简通栏）不再询问，避免打扰重看终局。

## #31 2026-08-25 — 对象绑定新手引导：滑动手势、聚光揭示与染色预报

- 改了什么：
  - 新增棋盘内非阻塞教练层：CSS 程序化触摸指示器循环演示滑动方向，聚光圈直接锚定玩家、色珠、中心、共振键或特殊格；说明卡自动放到目标另一侧，整层 `pointer-events: none`，不阻断真实触摸 / 拖拽。
  - 01 从一步改为两拍（`par` 1→2）：玩家先按手势滑到固定进攻位；到位后聚光四元中心，说明四条色臂、白箭头、背面撞入与 180° 翻转，再给第二段撞击手势。引擎规则不变，solver 唯一最短解更新为 `S,E`。
  - 03 拾珠前聚光游离色珠，拾取后自动切到中心与彩色落点；04–05 在合法进攻位显示“放入 / 换出”两行实时预报。撞错面、墙或边界继续用同一小卡解释原因；长按时显示预演未提交状态。
  - 后续首次机制复用同一视觉语法：10 共振键、16 光照格、17 分步目标、21 三臂空穴、27 弹射中心（就位后再补弹道提示）、33 阶段护罩、41 飞珠撞结构、43 再生护罩；均只在首拍解释对象与因果，行动后自动让位，不给多步答案。
  - 新增纯函数 `src/shell/tutorial.ts` 与 14 项测试，覆盖状态推进、放入 / 换出推导、错误面反馈和八类首次机制聚光。页面底部新增 `© 2026 Renatus Madrigal`，署名链接到 `https://github.com/AI1379`。
- 为什么：棋盘外静态文案要求玩家自己把文字映射回抽象图形，尤其手机玩家甚至还没建立“在棋盘上滑动”的输入模型。对象绑定教学让“看到什么—做什么—发生什么”在同一视区连续发生；第 1 关新增的一格移动承担输入学习，不作为走路税。
- 如何验证：`pnpm solve chem level-01` 得到 2 步 `S,E`，`audit:chem` 为 50% 决策密度 / 最长通勤 1；zod / BFS 基线与引擎、渲染测试同步更新，339 项 vitest、`pnpm typecheck`、`pnpm build` 全部通过。无头 Chromium 以 390×844 实际执行触摸拖拽，确认行动从 00→01、教练层由玩家滑动切换到四元中心揭示；逐屏截图复核 03 染色、10 共振的聚光位置、卡片避让、静态 hint 默认收起与底部署名。
- 遗留问题：程序化截图只能证明定位与交互生效，不能替代真人理解门槛；仍需按 `docs/playtest.md` 让未接触项目的手机玩家复述四元中心、染色双向交换及后续机制因果，并记录聚光卡是否消失过快。

## #32 2026-08-25 — 第一关按真实输入方式差分 WASD / 滑动教学

- 改了什么：第一关教练层新增键盘版与触屏版；首屏根据主指针精度和触点能力做保守预判，之后由真实方向键或 Pointer Events 输入即时覆盖。键盘版在棋盘对象旁循环显示对应的 W/A/S/D 按键，触屏版保留手指滑动轨迹；两版共享同一语义方向、聚光对象与状态推进。
- 为什么：视口宽度 / 比例只能说明页面布局，不能区分窄窗口电脑、平板外接键盘和触屏笔记本；UA 也不能说明玩家此刻使用哪种输入。能力预判加真实输入覆盖可以在不要求玩家选择设备的前提下自动纠错。
- 如何验证：新增 3 项输入差分测试（教程模型现共 17 项），覆盖能力预判、pointerType 覆盖与第一关 S→D 文案推进；`pnpm typecheck`、全量 342 项 vitest、`pnpm build` 通过。无头 Chromium 分别以 fine pointer / 0 触点和 coarse pointer / 5 触点进入第一关：前者显示 `S` 并隐藏手势，后者显示 `SWIPE` 并隐藏按键；触屏上下文真实按 `S` 后立即切为键盘模式并显示下一拍 `D`，桌面上下文收到真实 touch pointer 后也能反向切回滑动提示。截图同时复核按键与手势未遮住棋盘对象。
- 遗留问题：真人盲测仍需观察二合一设备上首次提示是否符合玩家当下意图；真实输入会立即修正，但首屏预判不承诺识别“设备身份”。

## #33 2026-08-25 — 第三关把预演操作并入第一次染色教学

- 改了什么：第三关拾取紫珠并到达进攻位后，教练层先要求按住对应方向键或在棋盘上拖住不松手；预演触发后再展示单向“放入”预报，并把 ghost 构型解释为松手后紫珠进入开口、随中心翻到对面的结果。松手执行、移开 / Esc 取消继续复用全局输入模型，不增加关卡状态或强制弹窗。
- 为什么：预演原本只散落在提示文字里，玩家可能在理解染色前已经直接完成第三关；把第一次预演与第一次持珠进攻绑定，可以让“怎么查看结果”和“染色究竟改变什么”在同一次真实操作中建立联系。
- 如何验证：扩充第三关教程模型测试，断言拾珠后只显示 HOLD 指令且不提前展示预报，收到 preview 事件后才出现 ghost 解释、单向放入结果与取消提示；`pnpm typecheck`、全量 342 项 vitest、`pnpm build`、`git diff --check` 通过。无头 Chromium 桌面场景以 `D` 拾珠、按住 `S` 380ms，触屏场景以右滑拾珠、向下拖住：两者预演前均隐藏结果，预演中行动数保持 01、手持仍为紫，卡片同步显示“紫珠 → 翻转后上侧”；截图复核键盘 HOLD / 触屏停留动画与 ghost 构型同屏可见。
- 遗留问题：非阻塞教学允许熟悉玩家快速点按跳过预演；真人盲测需观察新玩家是否会按提示停留到 ghost 出现，以及能否在松手前说出紫珠最终方向。

## #34 2026-08-25 — 游戏页收进单一视口，外围棋盘允许纵向压缩

- 改了什么：游戏壳改为 `100dvh` 内的固定行布局，舞台占用其余 UI 留下的空间；Canvas 外围不再强制 1:1，而按实际矩形尺寸重绘。棋盘格仍等比例，Canvas 绘制、chem 命中测试与教程聚光 / 手势共享动态逻辑宽高。选关弹层保持固定覆盖层，仅关卡列表内部允许滚动。
- 为什么：固定正方形 Canvas 加上标题、状态、提示、控制区与署名，在 900px 高桌面和部分手机上必然产生 body 滚动；真正可压缩的是矩形棋盘外的上下留白，不应通过 CSS 拉伸破坏圆形和点击坐标。
- 如何验证：新增 3 项矩形 Canvas 逻辑尺寸测试与 1 项 chem 矩形渲染 / 命中测试；`pnpm typecheck`、全量 351 项 vitest、`pnpm build`、`git diff --check` 通过。无头 Chromium 检查 1440×900、1366×768、390×844、375×667、844×390：document/body 的 `scrollWidth / scrollHeight` 均严格等于视口，教程聚光圈宽高比为 0.999–1.001；截图复核桌面、矮手机与横屏没有用裁切伪装无滚动。375×667 选关时页面仍为 375×667，只有关卡列表以 568px 可视区承载 4472px 内容并 `overflow-y:auto`；844×390 的通关评分卡压缩后 `clientHeight === scrollHeight === 322`，无第二个滚动容器或内容裁切。
- 遗留问题：当前最矮实测为 390px 横屏、最窄实测为 375px 竖屏；更小的目标设备仍须纳入发布前设备矩阵，但不得通过恢复页面滚动兜底。

## #35 2026-08-25 — 关卡进度本地持久化（localStorage）

- 改了什么：
  - 新增 `src/shell/progress.ts`：版本化 key `lexin.progress.v1` 读写 `{ completed: string[], current: Record<game, index> }`；读取时清洗非法字段，损坏 JSON / 隐私模式 / 配额满都静默回退或忽略。
  - 启动时从 `localStorage` 恢复已通关关卡和每个游戏最近打开的关卡；通关时写入 `completed`，打开 / 切换关卡时写入 `current`。
  - 已通关标记键从纯序号改为 `game:levelId`（缺省 id 时回退序号），关卡重排后进度更稳。
  - 新增 `progress.test.ts` 5 项，覆盖空存储、读写回环、脏数据清洗、去重与按游戏独立保存。
- 为什么：之前的 `completed` / `gameIndices` 都在内存里，刷新即丢。纯静态页没有后端账号，这种轻量进度用 `localStorage` 就够；`sessionStorage` 不跨标签 / 重开，IndexedDB 对当前数据量是过度工程。只持久化完成标记和最近打开关，不把整盘 `History` 拖进存档，保持协议最小。
- 如何验证：`pnpm typecheck && pnpm test && pnpm build` 全绿（351 项 vitest）；手工在浏览器通关一关后刷新，选关面板保留 ✓，且停留在上次打开的关卡。
- 遗留问题：尚未持久化未完成局面的撤销栈；若后续需要「关内断点续玩」再升级到存 `History` 或 IndexedDB。玩家标记仍按 §11 保持会话内，不跨会话。

## #36 2026-08-25 — 移除每关提示中重复的染色规则卡

- 改了什么：删除折叠提示区常驻的“染色 = 交换 + 翻转”说明卡及其按 `groups` 判断显示的壳层逻辑；提示标题统一恢复为“本关提示”，只显示关卡 JSON 自己的 `hint`。
- 为什么：第三关预演和第四、五关交换预报已经承担染色教学，后续每个含游离珠的关卡重复同一规则没有新增信息，还会挤占单视口布局和本关特有提示的注意力。
- 如何验证：残留搜索确认壳层已不存在 `mechanic-note`、`hasColoring`、“本关提示 · 染色规则”或配套 `flow-dot` 样式；含 / 不含游离珠的关卡共用同一简洁提示结构。`pnpm typecheck`、全量 351 项 vitest、`pnpm build`、`git diff --check` 全部通过。
- 遗留问题：染色规则的理解仍需按 01–05 盲测门槛验证；若玩家遗忘，应优化首次教学或按需复习入口，不恢复每关常驻重复。

---

## #0 2026-08-23 — 仓库骨架与双原型 v0

- 改了什么：
  - 建立仓库骨架：`src/core`（GameDefinition 协议、通用 BFS solver、撤销栈、关卡加载、键盘映射、补间工具）、`src/games/t3`、`src/games/chem`、`src/shell`（游戏切换 / HUD / 撤销 / 重开）。
  - 两个原型各实现 v0 规则与关卡 1：
    - `t3`：当前棋子 + 延迟 2 的红色回声棋子，同时到达各自目标格获胜；被墙挡住的输入仍消耗回合并进入回声队列。
    - `chem`：单中心四臂 + 开口臂；背面进攻成功触发四臂 180° 对调、开口臂翻转（占位规则，见 design §5 简化声明）。
  - 建立文档体系：`docs/design.md`（设计思路，含候选方案淘汰记录、双原型规则定义、50 关曲线、AI pipeline）、本文件（迭代演进）、`AGENTS.md`（协作规范，含文档维护硬性要求）。
  - 工具链：Vite + TS(strict) + vitest + zod；`pnpm solve` 命令行 solver。
- 为什么：比赛时间紧，先用同一骨架并行验证两个候选机制（「第 5 关测试」定题），骨架与 solver 无论哪个胜出都不浪费。
- 如何验证：`pnpm typecheck` / `pnpm test`（引擎手动解、阻塞回合、solver 最短解）/ `pnpm build`；`pnpm solve t3 level-01` 与 `pnpm solve chem level-01` 均可解。
- 遗留问题：
  - 两个原型都只有 1 关；下一步各手画 5 关跑测试。
  - `chem` 是占位规则，正式化需重设计（基团搬运等，见 design §5）。
  - `t3` 棋子碰撞规则未定（v0 允许重叠）。
  - 渲染暂无补间动画（`core/tween.ts` 已备好）。

## #1 2026-08-23 — 命名定稿与视觉包装方向

- 改了什么：
  - 双原型正式命名：`t3` → **《t+3》**、`chem` → **《109.5°》**（四面体键角），构成「符号双联」；命名理由与原则见 design §10。
  - 确定视觉包装方向：零美术素材的轻包装（排版 + 配色 + 程序化 Canvas 图形）；《t+3》偏科幻/时间感，《109.5°》偏化学味（分子骨架风格，但游戏内仍不出现化学术语）。
  - 同步更新 `README.md`、`AGENTS.md` 与 shell 标签。
- 为什么：比赛不要求剧情与美术，但需要最基本的辨识度与答辩记忆点；名字本身承担一半教学功能，化学梗留给答辩揭晓。
- 如何验证：纯文档与标签变更，不影响引擎行为。
- 遗留问题：标题画面与主题化渲染（残影 / 分子骨架风格）尚未实现，按 design §10 方向排期。

## #2 2026-08-23 — 《t+3》教程批次 01–10 + 构造验证工具

- 改了什么：
  - `t3` 新增 9 关（`level-02` ～ `level-10`），`level-01` 补充教学文案；前 10 关构成完整新手教程曲线（每关只教一件事，细化表见 design §6）：
    | 关 | 教什么 | 最短解 |
    | --- | --- | --- |
    | 01 迟到的同伴 | 发现回声、垫拍等它 | 4 |
    | 02 撞边也算数 | 撞边/撞墙消耗回合，原地踏步 = 等待 | 4 |
    | 03 时差 | 最后两拍未被回声接收，可各走各路 | 4 |
    | 04 绕行 | 绕路被原样重放 | 8 |
    | 05 同键不同路 | 精确安排被墙吃掉的拍子 | 5 |
    | 06 一拖二 | 两枚同延迟回声 | 5 |
    | 07 三层时间 | d2 + d4 双时间层 | 6 |
    | 08 缝隙 | 墙 + d2 + d4，目标倒推路线 | 8 |
    | 09 喂招 | 撞边的拍子成为回声的真实移动 | 6 |
    | 10 四层合奏 | 三回声 d2/d4/d6 组合 | 10 |
  - 关卡格式新增可选 `hint` 字段（t3 与 chem 的 `level.ts` 均已接入；zod 可选），shell 在棋盘外展示（`#level-hint`）。决策与理由见 design §4。
  - 新增 `scripts/craft-t3.ts`（`pnpm craft:t3 <spec.json> [--write]`）：从「预期解序列 + 起点 + 墙」用真实引擎正向构造关卡（末位置即目标格），再自动 zod 校验 + solver 核对。这是 design §7 pipeline「生成 → 校验」环节的第一块实现。
  - 新增 `src/games/t3/levels.test.ts` 批次守护：10 关全部通过校验、可解、最短解长度断言、教程每关必须带 hint 且不得泄露解法序列。
  - **零引擎改动**：多回声、不同延迟完全由关卡数据承担（`echoes[]` 本就支持）。
- 为什么：按 design §6 曲线推进教程批次；构造工具保证「预期解可行」与「最短解符合意图」都有机器证据，供后续 40 关复用。
- 如何验证：`pnpm typecheck` / `pnpm test`（36 tests）/ `pnpm build`；`pnpm solve t3 level-01..10` 全部可解且最短解长度与断言一致。
- 「预期解 vs 实际最短解」留档（pipeline 迭代证据）：
  - level-04 预期 `ENEESEWE`，solver 给出等长变体 `NEEEESNS`（先绕后入的同构绕法）；
  - level-09 预期 `SSSEEE`，solver 给出 `ESSSEE`——「撞边喂招」机制仍是必选，只是喂招前的走位自由；
  - level-10 预期 `WWESEESEEE`，solver 给出另一条 10 步对齐；多解但同深度，说明机制未被绕开。
- 遗留问题：
  - 碰撞规则仍为 v0（允许重叠）：level-08/10 存在棋子同格交错，若将来改互斥需重新验证全批次。
  - level-10 solver 访问约 18 万状态，接近默认上限；后续更深关卡注意控制棋盘规模或调高 `maxVisits`。
  - 11 关起按 design §6 曲线继续（压力板 / 回声瓦片等需要引擎扩展，留给定题后）。

## #3 2026-08-23 — 《109.5°》教程批次 01–10 入库（1 关 → 10 关，零引擎改动）

- 改了什么：
  - 《109.5°》关卡 1 → 10。01–05 教学段：第一次撞入 / 站到背面去 / 读开口方向 / 墙与绕行 / 完整路线规划；06–10 多中心段：两个中心 / 顺序有代价 / 别碰已达标中心 / 房间与门 / 综合毕业（每关教学点见 design §5 批次表）。
  - `hint` 字段在 chem `level.ts` 接入（与 #2 同一决策），全部 10 关均带教学/点拨文案；shell 提示条（#2 的 `#level-hint`）对 chem 生效。
  - 关卡 id 由 `chem-XX` 更名为 `109.5°-XX`：HUD 会展示关卡 id，旧代号在游戏内泄露「化学」字样，违反 design §10「游戏内不出现化学术语、标题是第一个揭晓点」。
  - 新增 `src/games/chem/levels.test.ts` 批次守护：全部关卡 parse 校验 + 初始未胜（无空关）+ solver 可解 + 最短解长度基线断言（1/5/4/7/10/13/13/7/19/20 步）。
  - design §5 记录 v0 **构型奇偶性结构上限**（每次进攻都是同一 180° 对换 ⇒ 每中心仅两种构型），§6 曲线 06–10 段实现方式相应调整——这是「第 5 关测试」的关键输入。
- 为什么：推进「第 5 关测试」——手画关卡验证 v0 机制深度；教程不靠剧情，只靠关卡顺序 + `hint` 点拨。
- 如何验证：`pnpm solve chem level-01..10` 全部可解且最短解长度与断言一致；level-07「顺序有代价」用约束 BFS 单独实测（先撞 A 13 步 / 先撞 B 19 步，顺序差 6 步）；`pnpm typecheck` / `pnpm test`（67 tests 通过）/ `pnpm build`。
- 「预期解 vs 实际最短解」留档：各关手算路线与 solver 最短解一致或等长（如 level-06 南侧路线与 solver 的北侧路线同为 13 步）；机制（背面进攻）按奇偶构造目标，结构上不可绕开。
- 遗留问题：
  - v0 上限下，需要翻转的中心必然恰好撞一次即达标，路线类深度接近极限：11–20 段扩充前需先定正式规则（基团搬运等，见 design §5 结构性上限）。
  - 教程提示为每关静态一条；是否需要分步提示待试玩反馈。

## #27 2026-08-25 — 正式定题 Chem，重组 40–50 为单一终盘曲线

- 改了什么：
  - 正式选择《109.5°》作为唯一比赛提交作品；`t+3` 转为研发档，不删除历史代码，但正常首页不再显示原型切换。首页品牌、HTML 标题、默认游戏和 README 全部切到《109.5°》；只有显式 `?game=t3` 才进入旧原型，`&devGames=1` 才显示研发切换器。
  - 保留已验证的 01–39，不为重排而重排；解决原曲线“44 已叫终局、45 又重新教学”的假结尾。新终盘为：40 旧规则复习；41–42 弹射撞核 / 撞光教学；43–45 关闸保形 / 回授闸门 / 护罩缓冲；46 撞核接回共振链；47–49 回到弹射资源 / 阶段预调 / 三路闸门综合；原 44「三段闭环」移至 50，成为唯一终局。
  - 机械重编号旧 41–50 的 JSON 与内部 `id`，映射为：旧 45→41、46→42、47→43、48→44、49→45、50→46、41→47、42→48、43→49、44→50。同步最短解基线、机制段落契约、去罩对照、反光格乒乓测试、渲染测试和盲测代表关。
  - 选关面板增加九个章节标题与编号范围，玩家能直接看到 50 关的学习结构；最后两章明确为“结构碰撞与回授闸门”及“综合终盘”。
- 为什么：Chem 已有 50 关实际扩展证据，原创性与关卡定理均强于仍停在 10 关的 t3；继续双原型会让评委默认打开不满足数量要求的版本。终盘重排让新规则只教学一次，随后回到旧规则综合并升压至唯一终局，消除两次起跑和两个结尾。
- 如何验证：41–50 逐关 BFS 最短解为 8 / 8 / 7 / 9 / 10 / 9 / 10 / 6 / 7 / 12，与重编号 JSON `par` 一致；新增文件序号 = 内部 id、1–49 不得命名“终局”、50 必须为三阶段终局的守护。267 项测试、`pnpm typecheck` 和 `pnpm build` 通过；全 50 关审计无新增通勤，重排只改变教学顺序，不改变已验证的局面和解。
- 遗留问题：定题不等于真人验收；01–05 规则复述、各机制段首次预测、移动端 / 色弱和 41–50 新终盘顺序仍须按 `docs/playtest.md` 找未接触玩家验证。AI pipeline 还缺候选生成 / 淘汰批次证据，不因本轮定题自动视为完成。

## #28 2026-08-25 — UI 字体与科幻仪表语法统一，通关卡片避让终局动画

- 改了什么：
  - 中文标题 / 正文统一到显式系统中文无衬线栈，英文眉题与纯读数使用独立技术字体栈；数字启用等宽数字特性，移除中文落入西文字体后再逐字回退的混杂观感。
  - 阶段目标、阶段护罩和共振预演编号从“小圆 + 粗等宽字 / Unicode 圈号”统一为 `01 / 02 / 03` 切角读数牌；Canvas 中文说明与数字也分别使用稳定字体栈。方向、撤销、重开、标记、提示与装饰图标改为 CSS 几何图形，避免系统符号字形差异。
  - 壳层面板减少大圆角，增加克制的蓝色信号线、低亮背景网格和切角读数语法；棋盘本体和玩法颜色不变，仍可用装饰开关关闭 Canvas 非玩法装饰。
  - 渲染层新增只读动画剩余时间，覆盖行走、翻转、共振阶梯、弹射飞珠、护罩碎裂与换手脉冲；通关卡片等待全部终局动画结束，再停留 360ms 后出现。撤销、重开、换关会取消待展示计时器。
  - 修复首次从 `?game=chem&level=XX` 进入时，初始化 `loadGame` 用默认 index 0 覆盖深链接关卡的问题。
- 为什么：截图反馈显示小圆里的 `1 / 2` 字重、字面框和基线都不稳定，整页也因大量同款大圆角面板更像通用网页后台而非结构分析仪；通关卡片与胜利动作同帧出现还会遮住“为什么达标”的关键反馈。
- 如何验证：268 项 vitest、`pnpm typecheck` 通过；无头 Chromium 以 927×1277 实际截图验收第 38 关字体、`01 / 02` 读数牌、图标与响应式布局。真实键盘事件验证胜利后 300ms 卡片仍隐藏、750ms 已出现；胜利后 100ms 重开并等待 800ms，卡片保持隐藏且行动归零。`?game=chem&level=38` 实际打开第 38 关。
- 遗留问题：本轮是开发者视觉验收，不替代真人盲测；阶段牌是否能在 1 秒内被理解为阶段配对、移动端 360–430px 宽度和色弱模式仍需按 `docs/playtest.md` 记录真实受试者结果。

## #29 2026-08-25 — 综合候选池扩到 60 关，建立先增后删基线

- 改了什么：
  - 新增 51–60 十张可玩 JSON 候选，不增加 schema 或操作：双锁撞核、缺口缓冲、双态闸门、解锁撞链、双喷口接力、三段注入、三路余波、光启缺口、三响余料、双锁回收。01–50 继续作为正式曲线，51–60 明确标为候选池；选关面板增加对应章节，50 改为普通 mastery，60 暂作候选终局。
  - 草案逐步回放后修掉三类假综合：51 / 60 的远端中心原可被直接进攻绕过，现校轴后必须由飞珠撞核；58 原本可重复踩光直取目标，现改为单次光照预调 + 三阶段开→关→开反馈，空穴必须往返两次；52 原只是在 45 上删一条臂，现增加必须等缺口复位才接通的链尾。
  - 最短解测试扩到 60 关，并为候选增加逐关机制事件集合、51 / 54 / 60 指定撞核不可绕、52 关盾挡飞珠与缺口开链、53 互补换闸、55 双喷口输出接力、58 单光反馈、57 / 59 / 60 余料终投等语义约束。`docs/playtest.md` 增加正式关↔候选关的成对删选协议。
- 为什么：41–46 仍主要承担 v4 教学，原曲线只有 47–50 四张真正的综合题，样本太少，容易在没有横向比较的情况下过早把某个组合当成终盘答案。先做十张有明确对照职责的候选，再用机器证据排除摆设机关、用真人预测质量决定删留，比直接凑满 60 或凭直觉删关更可靠。
- 如何验证：51–60 BFS 最短解依次为 **9 / 10 / 4 / 12 / 14 / 12 / 11 / 11 / 8 / 12**，均与 JSON `par` 一致；`pnpm audit:chem` 中候选决策密度为 36%–75%，除 54 因跨区回收飞珠保留 4 步有意义换位外，其余最长连续通勤均 ≤3。`pnpm typecheck`、318 项 `pnpm test`、`pnpm build` 全部通过。
- 遗留问题：51–60 尚未真人盲测，不能把“机器可解且语义成立”写成全部保留；下一轮按 playtest 的 9 组对照记录关键预测、撤销 / 提示、首通时间与重玩偏好，再删除结构同构或 UI / 记忆负担占主导的弱关。54 的 4 步换位也要重点观察是否被玩家体验为物流规划还是走路税。

## #26 2026-08-25 — v4.1 再生护罩返工：从条件门锁改为主动闸门

- 改了什么：
  - 不改 `reactiveTo` 引擎规则，重做 47–49 的玩法职责：47「关闸保形」（7 步）主动关盾隔离会烧坏已达标中心的共振，完成后再开盾；48「回授闸门」（9 步）把控制臂 / 闸门 / 链尾收进同一最终目标，保留闭→开→闭→开的反馈脉冲；49「护罩缓冲」（10 步）先关盾挡下一发 `hitCenters` 撞核，利用落地飞珠完成物流后再开盾。
  - 删除旧 47 的三段“照指令翻走再翻回”和旧 49 的一次性开锁结构；三关 hint 改为只指出危险关系，不再给动作顺序。
  - 再生罩打开时新增断开的休眠六边形与常驻 `R` 徽标；受保护中心到控制臂新增目标色虚线，控制臂匹配时显示实心端点。按住预演同时覆盖再生罩生成与消失，修复原先把再生罩解除预演传成 `undefined` 阶段色的问题。
  - 语义测试从“护罩状态变过一次”升级为具体契约：47 开→关→开且关盾真实阻断共振；48 闭→开→闭→开；49 在关盾期间发射且目标不被撞翻。47 / 49 去掉 `reactiveTo` 后均不可解，同一步去罩对照会分别烧到 / 撞翻受保护中心。
- 为什么：旧关只证明机关会开合，没有让“主动生成护罩”产生收益；旧 49 还退化成阶段护罩的换皮。开盾状态下完全不显示控制关系，玩家也无法预测哪条臂会让哪座罩变化。返工后，三关分别让护罩承担共振隔离、反馈闸门、弹射缓冲三种不可互换的职责。
- 如何验证：47–49 最短解分别为 7 / 9 / 10，逐步回放与 JSON `par` 一致；266 项测试、`pnpm typecheck`、`pnpm build` 均通过。全 50 关 `pnpm audit:chem` 通过，47 / 48 / 49 决策密度为 43% / 33% / 60%，最长连续纯移动为 3 / 2 / 2。
- 遗留问题：常显控制线与休眠罩仍需未接触规则的玩家盲测；重点观察能否在打开状态下指出控制臂，以及是否会主动想到“关盾是保护动作”而不只把盾理解为障碍。

## #25 2026-08-25 — v4 机制关卡 45–50 入库，正式曲线回到 50 关

- 改了什么：
  - 新增 6 个 v4 扩展关并排入正式曲线：45 弹射撞核（8）、46 撞光又撞核（8）、47 再生护罩（9）、48 再生闸门（9）、49 修中间体再撞核（11）、50 撞核共振链（9）。
  - 45 教「弹射珠撞中心进攻面 = 纯翻转，撞完的珠仍可复用」；46 教「先落光照转轴、再顺势撞中心」；47 教「再生护罩随中间产物开合」；48 教「修好中间体让共振链穿过闸门」；49 是再生护罩 × 弹射撞核组合；50 是弹射撞核 + 共振链 + 珠回收闭环毕业。
  - 测试新增两条 v4 守护：含 hitLights/hitCenters 的关最短解必须真的使用弹射中心发射；含 reactiveTo 的关最短解必须发生再生护罩开合变化。
  - 文档同步：design.md 曲线/P4/§6、playtest 代表关、AGENTS.md P6 均更新为 50 关。
- 为什么：把 v4 机制从“引擎就绪”推进到“有正式教学关卡”，并补回 50 关目标；关卡刻意避开光照乒乓，45/46 的落光照只踩一次（46 捡珠再转一次为设计一部分）。
- 如何验证：263 项测试全过；45–50 逐一 `pnpm solve` 可解且 JSON par 与基线一致；`pnpm typecheck && pnpm test && pnpm build` 通过；`pnpm audit:chem` 复核无异常长通勤。
- 遗留问题：45–50 尚未真人盲测；46 的“捡起光照格上的珠会再触发一次转轴”是否会造成误解，需在盲测中重点观察。

## #24 2026-08-25 — v4 新机制：弹射打结构联动与护罩再生（引擎 + 测试就绪）

- 改了什么：
  - 引擎新增两个可选机制，均不进正式 44 关，旧关零影响：
    - 弹射中心新增 `hitLights` / `hitCenters` 开关：弹射珠落在光照格上触发转轴；落在某中心当前进攻位时触发该中心纯翻转并继续共振传播。光照触发先于中心撞入判定。
    - 中心新增 `reactiveTo` 护罩再生：当所引用的“中间产物”臂不匹配声明颜色时，该中心进入护罩状态；修复中间产物后护罩立即消失。可与 `shieldUntilStage` 叠加。
  - zod 校验：`hitLights` / `hitCenters` 仅允许用于 `ejects` 中心；`reactiveTo` 必须引用存在的真实臂与可用颜色。
  - 渲染：再生护罩显示为带 `R` 徽标的六边形罩；普通阶段护罩仍显示阶段编号。
- 为什么：实现 design 留档的两条未来机制，让“弹射珠不是终点而是触发器”、让“护罩从永久解锁变成可被中间产物状态控制的可逆闸门”，为后续 40–44 之外扩展关铺路。
- 如何验证：新增 4 项引擎测试（落光照转轴、落进攻位撞翻、未开启不撞翻、护罩再生破坏/修复），243 项测试全过；`pnpm typecheck && pnpm test && pnpm build` 通过；`pnpm audit:chem` 对既有 44 关无变化。
- 遗留问题：尚未制作使用这两个机制的正式关卡；弹射打结构的“撞中心”目前只做纯翻转，不做注入，是否加入注入版待后续关卡验证。

## #23 2026-08-25 — 第二轮瘦身：50→44 关，修换关动画与通关卡片遮挡

- 改了什么：
  - 按真人反馈删除 6 个与既有教学重复 / 意义不明 / 镜像同构的关：旧 7（插槽与奇偶锁，保留 6 的换手接力）、旧 16（同时双响，与 11/12/13 同型）、旧 21（照亮两步，最简组合无新决策）、旧 43（东西换路，纯镜像 27）、旧 45（双喷流接力，资源接力已在 33 教过）、旧 47（空穴分流保险，与 26 同为一步链穿三臂）。
  - 全部重编号：01–09 核心搬运 / 10–15 共振 / 16–20 光照分步 / 21–26 空穴 / 27–32 弹射 / 33–39 阶段护罩 / 40–44 mastery；同步 `id`、`levels.test.ts` 基线、分段结构测试、渲染冒烟测试、`AGENTS.md` 曲线、`design.md` P4/§6、`playtest.md` 代表关。
  - 修换关动画 bug：进入旧 12（新 11）时无因翻转动画，根因是渲染层用 `|moves 差|=1` 判断步进动画，11 一步通关后进入同尺寸下一关时被误判成连锁；新增 `resetChemAnim()` 并在 `openLevel` 中调用。
  - 通关卡片改为可关闭：卡片新增「查看棋盘」，Esc / 关闭后露出终局棋盘，底部出现精简通栏（再玩一次 / 下一关 / ✕）；通关后移动不再反复弹卡片。
  - design.md 留档两个未来机制方向（暂不实现）：弹射珠撞击结构联动、护罩在中间产物被破坏后再生。
- 为什么：删关解决“很像 / 没意义 / 无聊 / 重复”的体验问题；修动画让教学关不再有“开局白翻”的噪音；修卡片让玩家能看到自己怎么通的。
- 如何验证：`pnpm typecheck && pnpm test && pnpm build` 全过（239 项测试）；44 关逐一 `pnpm solve` 可解且基线一致；`pnpm audit:chem` 复核，超长通勤只剩 24（6 步）与 17 / 25 / 37（4 步）。
- 遗留问题：盲测仍需真人执行；弹射打结构与护罩再生两项仅留档，未进入 schema / engine。

## #22 2026-08-25 — 关卡顺序与教学文案修正：24–29 恢复基础→组合序，护罩首现归还 36，删除重复板

- 改了什么：
  - 24–28 按 design.md P1 既定教学顺序重排（只改编号与 `id`，板子不动）：24 缺了一臂、25 空穴断路器、26 空穴保险丝、27 南北换路、28 照过空穴。
  - 新 29「双染引链」（5 步）：纯空穴×搬运毕业关，染色换出的珠成为第二次染色的输入；护罩首次登场从 29 归还给 36（原 29 的护罩经回放证明在最短解中从未挡住任何东西）。
  - 删除与 29 完全同板的旧 41；40/41/42 重排为：40「开罩接空穴」（新设计，6 步）、41 开罩三响（原 42 前移）、42 毕业：延迟喷流（原 40 后移收束）。
  - 重设计 46「转轴回收飞珠」：一束光同时转正弹射轴与运送轴，19 步压到 10 步，删掉光照格乒乓；新增「最短解不得重复踏入同一光照格」守护测试。
  - 重设计 49「开罩接三路」：第 1 击连锁三条去处——空穴切断北支、护罩拦下东支；开罩后以一次空翻复位奇偶，第三击接通罩内中心。8 步→7 步，最长通勤 6→2。
  - 40 / 49 增加去罩对照测试：删除 `shieldUntilStage` 后最短解退化到 1 步（连锁第一击即贯穿），证明护罩是硬约束。
  - 按 §4「hint 只点拨」重写全部 50 条提示（约 25 条原为完整解法剧透，含 38 / 39 / 50 的全流程叙述）；删除 05 中泄漏盲测协议「放入 / 换出」术语的句子。
  - 文档同步：design.md P1 段落消自相矛盾、P4 表与走路税段更新、新增审查决策留档；playtest.md 空穴代表关 24/25/28→24/25/27。
- 为什么：审查发现 P4 重编号把组合应用排到基础教学之前、护罩在空穴段提前登场且为死摆设、29≡41 重复板、毕业题排在护罩段中间、46 含 6 步光照乒乓；多条 hint 违反「点拨不剧透」。
- 如何验证：258 项测试全过（新增 4 项机制语义 / 去罩对照 / 反乒乓测试，原 24–29 语义测试按新编号挂接）；`pnpm typecheck && pnpm test && pnpm build` 通过；`pnpm audit:chem` 复核：29 密度 60%、46 为 10 步 / 通勤 3、49 为 7 步 / 通勤 2，超长通勤只剩 27 / 43 两关。
- 遗留问题：49 的关键一击依赖玩家意识到第 2 击是「看似无用的奇偶复位」，盲测若在此卡壳需评估是否加一条点拨；05 提示改为提问式但保留「放入 / 换出」的测量目标，A 段盲测仍须严格执行。

## #21 2026-08-25 — v3.2 真人盲测协议与无提示视觉入口

- 改了什么：新增 `docs/playtest.md`，把剩余三项真人验收固化为可重复协议：受试者必须未接触项目；观察者只收集原话、不教学；01–05 记录两次持珠进攻预测与开放式规则复述；弹射 / 护罩做 1 秒纯视觉辨认；六个机制段使用固定代表关记录首次误解、关键动作预测、卡点分类与辅助使用。浏览器新增 `?game=chem&level=XX` 深链接和 `&blind=1` 纯棋盘模式，后者隐藏品牌、关卡名、状态、提示、控制区并禁用输入，避免机制名称泄露视觉测试答案。
- 为什么：只有“找人盲测”而没有统一流程，会让不同人的结果不可比较，也容易由开发者在无意中提示答案。纯视觉门槛尤其不能让“第一次弹出”“第二阶段开放”的标题先告诉玩家要找什么。
- 如何验证：`pnpm typecheck && pnpm test && pnpm build` 全部通过（255 项测试）；正常入口逻辑仅在存在查询参数时改变初始游戏 / 关卡，`blind=1` 的隐藏与冻结均局限在壳层，不改引擎、关卡或正式默认体验。
- 遗留问题：协议与记录载体已就绪，但仍缺真实受试者数据；必须按模板完成会话并根据结果返工或勾选，不能用开发者 / AI 自测替代。

## #20 2026-08-25 — v3.2 P4：正式 50 关曲线收口，41–50 mastery 入库

- 改了什么：将 Chem 正式曲线重排为 01–10 核心搬运、11–17 共振、18–23 光照 / 分步、24–29 空穴、30–35 弹射中心、36–42 阶段护罩、43–50 mastery。41–50 新增开罩接空穴、开罩三响、E/W 换路、照光填槽、双喷流接力、转轴回收飞珠、空穴分流保险、一束光开三层、开罩 N/S 换路与三阶段终局；没有新增 schema 字段或操作。旧 21 / 23–25 / 27–31 / 34 / 37 / 38 的有效语义按新段落复核并保留，回收 / 弹射台 / 钥匙格关彻底退休。关卡测试扩为 01–50：除逐关 zod、初始非胜利、solver 最短解与 JSON `par` 同步外，新增分段机制契约；所有含空穴 / 弹射 / 护罩的关都回放最短解，分别证明实际翻洞、实际清手产出飞珠、先按阶段解锁再于后续动作使用罩内中心。
- 为什么：40 关原型批次仍按“机制广撒网”顺序排列，不能直接作为比赛正式曲线；后十关需要证明删减后的少量规则足以继续深化。逐机制的语义回放比只锁最短步数更能防止等长捷径把目标机制绕开。
- 如何验证：50 个 JSON 全部通过 strict zod 与 BFS，最短解已写入 `levels.test.ts` 基线；`pnpm typecheck && pnpm test && pnpm build` 全部通过（255 项测试）。旧字段扫描确认 50 个正式 JSON 不含 `disposals` / `launchers` / `deprotections` / `shielded`。`pnpm audit:chem` 逐关输出决策密度 / 最长通勤：旧 31 最长通勤 6→2，11 / 12 / 17 删除纯点火通勤，35 为 3，40 为 3，终局 50 从草案 16 步 / 最长通勤 7 压到 12 步 / 最长通勤 3。保留的 19 / 21 / 26 四步换边与 28 / 43 / 49 六步换路均承担另一进攻面或空穴支路切换，不是墙路填充。
- 遗留问题：P0–P4 的代码与自动化已完成；P2 的“一秒辨认弹射中心 / 说出护罩解锁阶段”、P3 第 5 关后的换入换出复述，以及每个新机制段的未接触玩家盲测仍必须真人执行。自动化不能替代这三项，当前保持未勾选。

## #19 2026-08-25 — v3.2 P3：第 3 关进入取代，01–10 从路线题改为颜色物流教程

- 改了什么：正式重制 01–10。01–02 只保留背面进攻 / 读开口；03 拾珠并第一次取代，04 单独强调旧开口珠换手，05 跨中心取送，06 连续交换，07 对比轴臂插槽与垂臂奇偶锁，08 强制空手先行，09 复用原 15 的共享站位与克制，10 做紧凑三中心紫→蓝→绿物流毕业。移除旧 03–10 的墙路、回形路、房间与通勤题；为 03–10 增加最短解交互轨迹断言，不只锁步数。
- 为什么：旧批次到第 11 关才出现正式取代，前十关把核心玩法误教成路线寻路。新序列让玩家在第三关看到“放入 / 换出”，再逐步建立颜色接力与空手时序；墙不再承担纯通勤。
- 如何验证：01–10 最短解分别为 1 / 5 / 2 / 2 / 5 / 8 / 3 / 6 / 8 / 6；solver 轨迹确认 05 必须 `purple→A / blue→B`，06 必须 `purple→A / blue→B / green→C`，08 必须 `A:empty` 后才拾珠，10 必须三中心依次接力。`audit:chem` 显示 03–10 决策密度全部 ≥38%，最长连续纯移动全部 ≤3；全量测试 224 项通过。
- 遗留问题：第 5 关后的“玩家能主动说出放入什么 / 换出什么”必须由未接触规则的真人盲测，当前不勾选；若失败，优先改 03–05 提示与反馈，不继续堆规则。

## #18 2026-08-25 — v3.2 P2：弹射弹道与阶段护罩获得可预测视觉语法

- 改了什么：弹射中心获得区别于普通圆核 / 三臂三角核的菱形喷嘴核，并在开口对侧常驻随轴旋转的短尾迹和双箭头。引擎新增纯函数 `getEjectionPreview`，统一提供被顶出珠颜色、逐格路径、落点及紧邻堵塞；就位常态和按住预演都画完整弹道，正式执行沿同一路径播放飞珠且手持同步清空。喷口受阻改为射出侧交叉火花，不再使用普通撞面弧和玩家抖动。阶段护罩改为强双六边形轮廓 + 解锁阶段号 / 色，与未来段目标徽标共用编码；阶段完成时碎裂，预演完成阶段时显示将破裂但不追加追溯共振。
- 为什么：旧弹射中心只能从 hint 或撞后结果辨认，玩家无法在行动前预测“弹出什么、飞过哪里、落在哪里”；旧护罩也没有说明何时解除。把路径计算放在引擎纯函数中，能保证预演、动画和真实规则永远同源；阶段号 / 色把未来目标与对应护罩直接配对。
- 如何验证：引擎测试断言正常弹道的方向、颜色、完整 path / landing，以及身后第一格堵塞时的空 path；渲染测试覆盖弹射中心静态帧、就位弹道、按住预演、执行飞珠、喷口受阻，另记录 Canvas 文本确认护罩与未来段目标均绘制“2”，并验证阶段完成预演不改变罩内中心。`pnpm typecheck && pnpm test && pnpm build` 通过（216 tests）。
- 遗留问题：程序化渲染的自动测试只能防运行时与编码回归；“1 秒辨认弹射中心 / 看编号说出解锁阶段”的盲测验收仍需真人完成，当前不勾选该项。

## #17 2026-08-25 — v3.2 P1：空穴从缺臂外观升级为共振拓扑元件

- 改了什么：固定三臂中心始终三条真实臂、开口跳过空穴、空穴不参与拾取 / 注入 / 目标颜色 / 共振，且直接进攻或共振命中后都先整体翻转、再按翻转后拓扑继续传播。重制 24 / 27 / 28 / 29 四类语义关：断路器、保险丝、N/S 双路换向、阶段护罩解除后的拓扑扩张；26 保留为空穴 × 光照的正交组合。`trace:chem` 新增可选 N/E/S/W 指定轨迹，方便回放尚未可解的制关中间态。
- 为什么：单中心“少一臂”只有视觉差异；让空穴控制共振的接通 / 断开，才能在不增加操作语法的前提下产生可推导的两态网络。组合顺序先证明空穴 × 共振，再加入光照，最后加入阶段护罩，避免玩家同时解读四条规则。
- 如何验证：引擎测试显式覆盖两个相对 `undefined` 不会被当成同色键、持珠进攻后仍恰好三臂、光照跳洞、目标可指向初始空槽但颜色必须来自真实色珠。关卡测试回放最短解，逐动作断言洞的 W↔E / N↔S 变化与具体支路是否翻转；另做过滤关键语义转移的约束 BFS，证明 24 / 27 / 28 / 29 缺少指定接通或断开事件时不存在等长或更短解。最短解为 24=3、27=1、28=8、29=3；全量测试 214 项通过。
- 遗留问题：28 为绕过上下支路从中心西侧换到东侧，最长连续纯移动 6；该路程有拓扑站位意义，先保留到盲测。空穴静态轮廓与弹射 / 护罩反馈进入 P2，正式编号仍会随 50 关曲线最终复核。

## #16 2026-08-25 — v3.2 P0：删除外围功能砖，阶段护罩完成迁移

- 改了什么：从 Chem schema、运行态、`stateKey`、引擎、渲染与调试脚本彻底删除回收格、弹射台、独立脱保护格和全局 `deprotected`；schema 改为 strict，旧字段会显式报错。保护罩改为中心字段 `shieldUntilStage`，只在 `stage` 小于阈值时阻挡直接进攻与共振；光照仍能移动罩内开口。保留弹射中心 `ejects` 及其唯一主动恢复空手的语义。同步迁移 22 / 26 / 32 / 33 / 35 / 36 / 39 / 40，分别改作光照分步、弹射中心与阶段护罩组合题。
- 为什么：外围功能砖各自引入独立语法，却削弱「背面撞入—取代—翻转—共振」主线。阶段直接驱动护罩无需钥匙状态，且能把顺序门升级为连锁拓扑随进度扩张；strict schema 防止旧字段被 zod 静默剥离后形成难查的关卡语义漂移。
- 如何验证：新增 vitest 覆盖阶段前阻挡、阶段后直接放行、完成阶段当步不追溯、下一动作传播、光照穿透护罩、多护罩按不同阶段依次解除、旧字段拒绝及 `stateKey` 不含旧全局位；保留弹射中心正常 / 堵塞 / 空手测试。8 个迁移关逐关经 solver 验证，新最短解为 22=9、26=9、32=3、33=5、35=6、36=5、39=6、40=14；`pnpm typecheck && pnpm test` 通过（209 tests）。
- 遗留问题：阶段编号 / 颜色、护罩破裂和弹射中心弹道仍需在 P2 完成视觉表达；26 / 36 / 40 还要随 P1 空穴语义约束测试复核，正式 50 关编号与难度曲线尚未收口。

## #15 2026-08-24 — v3.2 收敛计划：删除外围功能砖，阶段护罩与空穴成为后期主线

- 改了什么：本条只冻结下一轮规则与关卡改造计划，尚未修改引擎和关卡。正式方案删除回收格、弹射台与独立脱保护格；保留光照、三臂中心、弹射中心和分步目标。保护罩改为随分步目标推进而单向解除的阶段护罩；三臂缺槽明确为不可填充、无颜色、不能共振、随 180° 翻到对侧的「空穴」。详细规则写入 design §5 v3.2，可执行清单写入根目录 `AGENTS.md`。
- 为什么：回收 / 炮台 / 钥匙格各自引入一套外围地砖语法，却没有像核心取代与共振那样产生足够多的结构定理；当前保护玩法尤其退化为走到远处踩锁。空穴则无需新增操作，已经自然具备断链、护尾、双路换向和阶段拓扑四类题型，符合「规则少、组合深」的定题方向。旧 01–10 的审计数据也确认正式玩法出现过晚，比赛开场必须从第 3 关就进入持珠取代。
- 如何验证：对照现有 schema / 引擎 / 21–40 关逐项盘点受影响内容：22/26 使用回收，32/35/39/40 使用弹射台，33/36/40 使用脱保护；31/34/38 的弹射中心可以保留，25/27/29 的三臂基础可以保留并深化。检查现有引擎已满足「缺槽不参与共振、翻转到对侧、光照跳过缺槽」的基础语义；本轮仅文档 / TODO 变更，未宣称代码已完成。
- 遗留问题：需按 `AGENTS.md` TODO 顺序完成 schema 清理、阶段护罩引擎、弹射中心 UI、01–10 重排和受影响关卡重制；阶段完成回合不追溯传播、光照可穿透护罩两条边界必须写入测试。空穴关还需用 solver 语义约束证明目标连锁确实因缺槽接通 / 断开，而非被其他路径绕过。

## #14 2026-08-24 — 13–15 / 18 / 20 走路税审计与关卡压缩

- 改了什么：
  - 新增 `scripts/audit-chem.ts` 与 `pnpm audit:chem [level-XX ...]`：回放 solver 最短解，区分成功进攻 / 拾取换珠 / 特殊格等「有效交互」与普通移动，输出决策密度、最长连续普通移动和搜索状态数。
  - 重排 13–15、18、20 的棋盘几何，只缩棋盘、移动中心 / 玩家 / 场珠并删除无意义隔墙，不改臂色、目标或引擎规则。最短解由 17 / 18 / 16 / 14 / 16 降为 **10 / 10 / 8 / 7 / 9**；决策密度由 24% / 22% / 19% / 21% / 19% 升为 **40% / 40% / 38% / 43% / 33%**；最长连续普通移动由 5 / 6 / 5 / 6 / 5 降为 **4 / 3 / 3 / 3 / 3**。
  - 关卡基线同步更新，并新增最短解语义轨迹测试：逐关锁定拿珠颜色、进攻中心、进攻时手持状态和次序，防止以后虽然总步数没变、核心教学却被等长捷径绕开。
- 为什么：19 关压缩后确认「难」常来自反馈延迟，而非推理本身。旧五关每关只有 3–4 次真正改变机制状态的动作，却用 11–14 步普通移动拉长；这会把颜色交换 / 奇偶点火判断埋在方向键 bookkeeping 中，对比赛试玩是减分项。
- 如何验证：`pnpm audit:chem 13 14 15 18 20` 对比压缩前后指标；五关逐一 `pnpm trace:chem` 回放，确认 13 的三次颜色接力、14 的开局空手时机、15 的共享站位克制、18 的同中心二次点火、20 的左右链奇偶序列仍真实发生。随后 `pnpm audit:chem` 跑完全 01–40 关，全部可解；额外筛出 17 / 31 / 36 / 40 供下一轮人工判读。`pnpm typecheck && pnpm test && pnpm build` 全绿（212 项测试）。
- 遗留问题：指标是报警器而非评分公式——01–10 本来就在教站位 / 路线，低密度不能机械判坏；17 / 31 / 36 / 40 的最长连续普通移动仍为 6 / 6 / 5 / 7，需逐关判断是有意义站位还是纯通勤。还需用未接触规则的玩家盲测 13–20，记录错误发生后到看懂反馈所需的回合数。

## #13 2026-08-24 — level-19「断链」降难：14 步路线压缩为 7 步二选一

- 改了什么：保留原 A→B→C 三中心链、C 初始已达标、红 / 蓝两珠只有一颗不会烧过链尾的结构；棋盘从 5×5 收到 4×4，把玩家放在 B 的进攻位、两颗候选珠放到左右相邻格。安全路线变为「选珠 → 染 B → 短路到 A 点火」7 步，提示改成直接要求比较哪种颜色不会接成蓝线。
- 为什么：旧版 14 步把真正的颜色判断埋在长距离取珠、绕行与两次染色后的工作记忆中；玩家觉得难，主要负担不是推理而是 bookkeeping。三中心已经是表达「共振会误伤已达标链尾」的最小结构，因此不删中心，只删路线噪音。
- 如何验证：`pnpm solve chem level-19` 最短解 7 步（访问 99 状态），`pnpm trace:chem level-19` 确认红珠路线按预期完成 B 染色 → A 点火 → B 传导且 C 保持 ✓。新增约束测试：红珠染 B 后剩余最短解 4 步；蓝珠染 B 后按教学路线会翻掉 C，恢复至少还需 16 步。`pnpm typecheck && pnpm test && pnpm build` 全绿（207 项测试）。
- 遗留问题：需真人确认 7 步版是否能让玩家把注意力放在蓝色连线比较上；若仍难，下一步应强化预演中的危险链高亮，而不是继续减少中心数量。

## #12 2026-08-24 — 三臂中心 v3.1：删除 mod 3，收敛为移动缺口的标准翻转

- 改了什么：
  - **先改规则决策**（design §5）：保留 `kind: 'trigonal'` 数据标识，但玩家语义从「三臂顺时针轮换、三撞复原」改为「四槽中缺一臂，仍整体翻转 180°、两撞复原」。三颗珠、缺口、开口箭头一起 N↔S / E↔W；持珠取代与普通中心完全同构。
  - **引擎去特判**：删除 `TRI_DIRS` / `triNext` / `triPrev` 和专属取代分支；四臂、三臂统一使用 Partial 四槽的 180° 映射。共振只遍历当前现存臂，移动缺口可以在翻转后接通 / 断开链路；`stateKey` 固定记录 N/E/S/W 四槽，缺口位置参与去重。
  - **光照兼容**：开口顺时针移到下一条当前存在的臂，三臂中心自动跳过缺口；校验要求开口初始附着在现存臂上。三臂中心可从任一方向缺臂，目标允许指向翻转后才出现的槽位。
  - **渲染与认知外置同步**：删除三臂沿弧轮换动画，复用标准刚体半圈动画；缺失位置画暗淡虚线空槽，避免与中心核内的进攻箭头混淆；Inspect 从三态收敛为与普通中心一致的「现在 / 翻一次后」两态，预演落点统一为开口对侧。
  - **关卡重制**：25「缺了一臂」2 步、27「三臂搬运」5 步、29「光与缺口」4 步、35「狙击」3 步、40「大毕业：双向贯通」14 步。40 关改为左侧发射完成第一段，中心与缺口翻面后右侧发射自然接手第二段。
- 为什么：三臂形状本身有价值，但原 mod 3 迫使玩家切换旋转语法、落点口诀和 Inspect 周期，属于机制爆炸。新版本用一句「和普通中心一样翻转，只是少一条臂」复用既有心智模型，同时让移动缺口自然产生共振拓扑与双向物流谜题。
- 如何验证：新增三臂任意缺槽 / 开口合法性 / 初始缺失槽目标 / 两次翻转恒等 / 持珠同构取代 / 缺口翻后接通共振等引擎测试；受影响五关全部 `pnpm solve` + `pnpm trace:chem` 核对。`pnpm typecheck && pnpm test && pnpm build` 全绿（206 项测试）。
- 遗留问题：需要真人确认暗淡空槽与白色进攻箭头是否能被一眼区分；三臂中心与光照 / 共振组合目前已有 29/40 与引擎证明，41–50 再决定是否需要一关专门深化「缺口控制链路」。

## #11 2026-08-24 — 认知外置层：时间线核心 UI / 按住预演 / Inspect / 标记（两游戏通用输入模型）

- 改了什么：
  - **设计原则入库**（design §11）：「Challenge reasoning, not memory——玩家的脑力应该用来发现规律和作出决策，而不是维护游戏本可以替他显示的状态」。三层认知外置（自动显示 / 一步预演 / 轻量标记）成为两游戏共同 UX 准则；确立定题补充判据「同等深度下，机制需要多少 UI 才能让玩家舒服地思考」与四问试玩实验协议。
  - **《t+3》时间线升级为核心玩法 UI**（`t3/render.ts` 重写底部条）：指令缓冲区网格——列 `t+1 … t+D+1`（D=最长延迟，末列=下一次按键的落点），每枚回声一行（延迟标签 +d、已确定的拍画彩色箭头、未输入的拍画 ·）+ 玩家行；`t+1` 列高亮。棋盘上每枚回声挂「下一步动作」chip。按住预演时：ghost 棋子（玩家 + 各回声下一拍位置 + 移动箭头）+ 时间线上该输入流入各回声的落点列（ghost 箭头）。level-01 hint 改写为「输入不会消失，它会沿时间线向后流动」。
  - **《109.5°》三件套**（`chem/render.ts` 扩展）：① **Inspect**——点按中心显示构型周期面板（tetra 现在/翻一次后；trigonal 0→1→2 全周期，当前态高亮），引擎新增导出纯函数 `peekFlip`；② **按住预演**——变化中的中心整体切到「动作后构型」ghost 态（虚线晕圈 + 共振链 ①②③ 徽标），手持换出物 / 场上珠增减画 ghost，目标 ✓ 锁定圈按预演后状态判定；弹射台 / 光照格 / 回收格 / 保护罩全部走通用 diff 覆盖；③ **标记**——M 键 /「标记」按钮进入标记模式，中心循环 ①–⑤、其他格循环 ★/？/×，会话内按「游戏:关卡」保存，不进引擎状态与 stateKey。
  - **预演动效跟手修正**：试玩反馈指出 chem 预演构型直接跳到结果不够顺滑；预演中心现复用正式执行的 tetra 半圈旋转 / trigonal 沿弧轮换与共振链 90ms 阶梯延迟，再停在虚线 ghost 结果。光照格仅转开口，继续不伪装成翻转。
  - **chem 棋盘减噪**：截图复核发现拥挤主要来自渲染层三组几何互相穿插，而非页面 CSS——目标虚线圈缩小且达标后自动降亮、臂原子轻量收径、达标锁定圈改为只环中心核；白色方向箭头确认是 Canvas 图形，最终重构为中心核内部的短箭头并置于最上层，随翻转 / 轮换动画旋转，不再占用臂端或被进攻位玩家遮挡。
  - **壳层输入模型统一重构**（`shell/main.ts`）：tap=执行、hold≥280ms=预演、松开=执行、Esc/移开=取消；键盘执行时机 keydown→keyup（回合制无可感延迟），换键滚动自动先提交旧方向；棋盘拖拽 ≥24px 即实时预演（随手指变向），<24px 视为点按（Inspect / 标记）；触屏方向键 pointer capture。预演 = 壳层对当前局面求一次 `step` 交给渲染层画 ghost，引擎与关卡零改动。
  - **刻意不做** Plan/Sandbox 任意步沙盘（design §11「辅助分层配置」）：无限撤销 + solver 提示 + 一步预演之上再叠沙盘 = 游戏自己帮你玩。
- 为什么：试玩暴露的不是零散 UX 毛病而是「我知道要干什么，但懒得在脑子里维护模型」——两候选机制都缺一层可思考性设计；t3 的工作记忆是一维输入时间线（外置便宜），chem 的认知状态是图（外置贵），这本身成为定题的重要输入。
- 如何验证：`pnpm typecheck && pnpm test && pnpm build` 全绿（新增：`peekFlip` 引擎语义测试、t3 渲染冒烟（时间线早/中盘/预演路径）、chem 渲染冒烟（预演翻转 / 共振阶梯 / diff / Inspect tetra+trigonal / 标记徽章））；全部关卡 solver 基线不变（引擎规则零改动，`peekFlip` 为新增纯函数）；`pnpm solve t3 level-01..10` 与 chem 抽查仍为既有最短解。
- 遗留问题：
  - 共振链 ①②③ 的编号是渲染层按相邻 BFS 距离的近似（与既有翻转阶梯动画同一口径），弹射台远程命中的编号顺序可能不精确——观感正确、语义近似，暂不修。
  - 标记不跨会话持久化（无后端，localStorage 暂不做）；t3 有意不加标记（时间线已完整外置）。
  - 四问试玩实验（design §11 协议）待找 3 位未接触项目的玩家执行，结论回填后定题。
  - 「tap=执行 / hold=预演」对高频快速连打的手感需真人实测（执行移到 keyup/pointerup，理论无感）。

## #10 2026-08-23 — 《109.5°》染色教学返工（规则不变）

- 改了什么：
  - 将玩家口中的「染色」统一定义为 **交换 + 翻转**：含游离色珠的关卡在教学面板常驻两条去向备忘——手中色珠进入中心并随它翻转；原开口臂色珠换到手中。
  - 重写 11–15 的教学递进：第一次完整染色 → 从开口换出目标色 → 连续交换的手持不变式 → 空手纯翻转与持珠染色对比 → 综合路线；18–19 的提示同步改用同一术语，19 明确要求保护已达标链尾并比较蓝色连接。
  - 修复阻碍理解的渲染几何：普通臂从相邻格中心缩回中心附近、目标圈跟随真实臂端，玩家站在进攻位时不再盖住即将被染色的臂。
  - 新增上下文落点预览：持珠站到合法进攻位时，从玩家到最终落臂显示手持颜色的虚线流向箭头、半透明色面与脉动光环；四臂 / 三元中心分别复用真实 `opposite` / `triPrev` 落点语义，只读状态，不预先执行引擎。
  - 渲染冒烟测试补充「持珠且站在合法进攻位」路径。
- 为什么：试玩到 19 关仍无法稳定预测染色结果，说明原 11–15 的长文案教学没有形成心智模型；更严重的是旧臂端与玩家共用相邻格中心，新颜色在关键观察时刻被玩家遮住。设计审查与新教学纪律见 design §5「染色教学返工」。
- 如何验证：`pnpm typecheck && pnpm test && pnpm build` 全部通过（193 项）；11–15、18–19 逐关 `pnpm solve` 仍为既有最短解 6/11/17/18/16/14/14，证明引擎、关卡图与解法未变化。
- 遗留问题：需要从第 11 关开始找一位未接触规则的玩家盲测，确认玩家能否在第 13 关前主动说出「我放入什么 / 会换出什么」；若仍失败，应拆出一步一屏的首次染色演示，而不是继续加长文案。

## #9 2026-08-23 — 《109.5°》UI / 交互审查与移动端收敛

- 改了什么：
  - **壳层信息架构重排**：旧版单条顶栏中的游戏切换 / 关卡 / 步数 / 提示 / 装饰 / 撤销 / 重开拆成品牌切换、关卡标题、状态条、可折叠教学、棋盘、主操作六层；关卡选择改为可滚动模态面板，当前关与本次会话已完成关有明确标记。
  - **状态去重**：删除 chem Canvas 底部会被裁切的长句 HUD；行动数、标准杆、分段、当前目标满足数、手持状态统一移到 DOM 状态条，Canvas 只画棋盘与即时反馈。
  - **触屏输入补齐**：新增四向触控键与 Canvas 主方向滑动（24px 阈值 + 斜滑过滤），保留方向键 / WASD / Z / R / H；按钮命中区不小于 44px，补充 `aria-label`、焦点态、禁用态与胜利焦点。
  - **chem 棋盘可读性**：玩家改为亮环暗芯轮廓；色珠用颜色 + 内部点/横线/竖线/叉/环双编码，目标圈也使用不同虚线节奏；开口箭头提高对比并从臂端出发；墙增加斜纹，棋盘与背景分层；回收原 Canvas HUD 空间给棋盘。
  - **完成反馈**：胜利卡展示关卡名、步数、标准杆与星级，提供重玩 / 下一关；上一关、下一关、撤销按实际可用状态禁用，切换原型时保留各自当前关位置。
- 为什么：旧界面是开发工作台而不是可交付 H5——主次操作等权、同一状态重复出现、Canvas 长文本在窄屏裁切、移动端没有方向输入，且色珠主要靠颜色区分。设计决策见 design §10「《109.5°》界面与交互收敛」。
- 如何验证：`pnpm typecheck && pnpm test && pnpm build` 全部通过（193 项测试；包含 chem 01–40 zod / solver 最短解基线与全部渲染路径冒烟）；引擎、规则、关卡 JSON 零改动。
- 遗留问题：仍需在真实 iOS / Android 设备做一轮拇指区、字体缩放与色弱人工试玩；当前自动化只覆盖渲染不抛错，不做像素快照。

## #8 2026-08-24 — 回退 UI 质感层 + solver 提示系统（玩家辅助）

- 改了什么：
  - **回退** 5e66ecd「UI 质感层」：试玩反馈改后反而更丑，`git reset --hard` 回到 d89739d 的观感。教训记档：视觉大改要先小范围出样给玩家确认，再铺开。
  - **提示系统**（design §10「玩家辅助」决策落地）：壳层新增 💡 提示按钮 / H 键。`core/solver.ts` 抽出 `solveFrom(def, state, opts)`（引擎零改动），提示从**当前局面**实时跑 BFS 取最短解第一步，浮层展示「下一步：→（右）· 距通关还有 N 步」。
  - **死局检测**：搜索空间穷尽仍无解 ⇒ 明确提示「这个局面走不通了，撤销或重开」——回收格扔错珠子这类不可逆失误第一次有了出口。
  - 截断兜底：求解超限时退回本关文字提示。提示不限次数、不影响星级。
- 为什么：玩家反馈「有些关卡做不出来」。手打攻略维护不起，solver 提示对两个游戏通用、零内容成本，且死局检测本身是教学。
- 如何验证：`core.test.ts` 新增 solveFrom 三用例（中途局面剩余解 / 已胜空解 / 死局穷尽非截断）；全套 `pnpm typecheck && pnpm test && pnpm build` 通过（193 项测试）。
- 遗留问题：提示只给「下一步」不给思路讲解；若试玩仍卡，考虑对高难关（19/20/40）补更细的关卡内提示文案。

## #7 2026-08-24 — 《109.5°》v3 机制群 + 21–40 批次（广撒网，试玩后再删）

- 改了什么：
  - **引擎 v3（design §5「v3 机制群」决策落地）**：7 条新机制——光照格（全场开口顺时针转轴，三元中心在三方向内轮转）、回收格（销毁手持，打破手持不变式）、保护基 + 脱保护格（罩住的中心进攻无效、共振不入 = 链闸；脱保护永久）、分步目标（`stages` 按段推进，一步可连进多段，旧 `goals` 规约为单段）、弹射中心（携带珠注入、顶出物沿反方向飞出，手变空）、弹射台（手持走入沿台面方向发射，远程取代时顶出物落回珠刚离开的格子）、三元中心（三臂轮换 mod 3，落臂 = 开口的下下家）。另加标准杆 `par`（纯展示 + 通关星级）。
  - **关卡 21–40 入库**（教学序列与基线 6/13/6/7/7/9/5/9/9/10/11/3/9/4/3/11/5/8/7/21 见 design §5 批次表）。全部 `pnpm solve` 验证 + 关键关 `pnpm trace:chem` 逐帧比对。
  - **渲染**：特殊格视觉（光照=金色放射纹呼吸 / 回收=虚线井口下箭头 / 脱保护=开锁 / 弹射台=双箭头）、保护罩六边形、三元中心三角骨架 + 沿弧轮换动画（非刚体旋转）、分步目标当前段亮圈 + 未来段淡圈预告、HUD 标准杆/段数/通关星级；连锁阶梯动画兼容新机制（光照只转开口不播翻转动画）。
  - **测试**：引擎 33 项（每条机制语义 + 回归），渲染冒烟覆盖全部 v3 视觉路径，关卡基线扩到 01–40（共 190 项测试全绿）。
- 为什么：玩家决策「机制多做、多体验、回头再删」——把头脑风暴的推荐档 + 次选档 + 深水区里的三元中心全部落地，用试玩反馈决定 41–50 段保留哪些。刻意不做空臂消除（推翻目标系统）与手性棋盘（状态翻倍），留独立验证周期。
- 制关证据与返工记录：
  - **弹射中心重复色 bug**：初版「手持不变」让携带珠注入后仍留在手里（颜色无中生有），引擎测试抓到，改为注入后手变空。
  - **翻转双轴定理再踩坑**：23 关首版按「只翻进攻轴」手算链色，实际翻转是 N↔S + E↔W 全对换——连锁同色条件全部重推。教训：链色一律以 solver/trace 为准，不手算。
  - **手持强制注入再现**（28 关）：A 先行方案被 A 换出的手持物砸坏 C 的目标，唯一序是空手先做中间体——与 19 关同构，确认为本规则族的稳定深度来源。
  - **目标初始即真陷阱**（36 关）：「翻转后复原」类目标初始就满足，被 7 步绕过；重设为「先撞立目标 → 解锁 → 反向连锁」。
  - **40 关 solver 优于设计**：solver 用顶出的蓝珠当周转换黄珠（换珠机制自现），21 步与设计基线一致但路线更精彩——保留并记录。
  - 站位几何再确认：开口 d 的站位 = 中心 − DIR_VEC[d]，进攻方向 = d（v2 期已定型，本批 20 关全部按此复核）。
- 如何验证：`pnpm typecheck` / `pnpm test`（190 项）/ `pnpm build`；21–40 全部 solver 可解、最短解与基线一致；22/23/24/26/28/31/33/36/38/40 经 trace 逐帧确认教学动作真实发生（回收、弹射落点、链闸、连锁、分步推进、发射×2）。
- 遗留问题：
  - 试玩反馈后做**机制删减**：候选观察点——弹射台与弹射中心是否冗余、三元中心的心算负担是否过重、标准杆星级是否有人看。
  - 41–50 段等删减结论后再排（组合深化或补新机制）。
  - 胜利回放 / 标题屏（包装项）未做。
  - AI 关卡 pipeline（§7）该启动了：50 关指标里 t3 仍只有 10 关，chem 40 关已验证的规则结构（奇偶/轴/链）适合模板化生成。

## #6 2026-08-24 — 《109.5°》v2 共振传导 + 16–20 批次 + 白圈 UI 修正

- 改了什么：
  - **引擎 v2（design §5「共振传导」决策落地）**：正交相邻且面对臂同色的中心连通；任一中心被翻转后，震动沿连通链传导纯翻转，一次连锁每中心至多翻一次、结果与传播顺序无关。进攻判定、动作空间、`stateKey` 零变化；01–15 无相邻中心 ⇒ 完全向后兼容（旧基线一步未变，测试证实）。
  - **关卡 16–20 入库**（教学序列与基线 4/7/14/14/16 见 design §5 批次表）：共振发现 / 多米诺逐级接通 / 奇偶点火 / 断链（危险色珠）/ 奇偶序列点燃双链。全部 `pnpm solve` 验证 + `pnpm trace:chem` 逐帧比对设计意图。
  - **UI 修正（试玩反馈：白色圈圈像 bug）**：开口标记由「白圈」改为**朝外的白色箭头**（自带方向语义；`level-01` hint 同步改写）；达标锁定圈加 **✓ 徽标**；相邻中心画**共轭键**（面对臂同色时点亮脉动，否则暗色）、相邻臂缩短避让、面向相邻臂的目标虚线圈移到半程位置；连锁翻转动画按传播距离阶梯延迟（每级 90ms）。
  - 测试：引擎新增 6 项共振语义测试（翻后判定 / 不通不传 / 多米诺 / 纯翻转不动手持 / 连锁按进攻重置）；渲染冒烟覆盖相邻中心与连锁转移；关卡基线扩到 01–20。
- 为什么：试玩反馈两条——①白圈无法理解、疑似 UI bug（玩法标记必须自带语义）；②携带机制仍不够有趣，要求连锁反应并多借鉴有机化学。共振 = 共轭体系的抽象，是 design 里原排 21–30 段的第一条「新规律」，提前到 16–20 落地。
- 制关中证明的结构定理与返工记录（已写入 design §5）：
  - **链环颜色不可投放定理**：面对相邻中心的臂永远无法成为投放落点（站位被邻居占据）⇒ 点火控制权只能是翻转奇偶。level-18 初版「先配平再点火」因此整体重构为「奇偶点火」。
  - **手持强制注入**：进攻必然把手中之物注入目标 ⇒ level-19 初版终点进攻被手持蓝珠砸坏 A 目标，A 目标改为「强制注入色」后反而强化了断链教学。
  - **双翻恒等式**：同中心两次翻转 + 无净投放 = 恒等 ⇒ level-20 初版「自提取物流」被 solver 用全空手双翻 14 步绕过；重构为「奇偶序列点燃双链」。
  - **回火**：被传导的中心会以新颜色复查邻居 ⇒ level-20 二版中 X1 被偶次翻转回火摧毁，改配色保证「翻后面对臂色 ∉ 邻居后续奇偶态」。
- 如何验证：`pnpm typecheck` / `pnpm test`（116 项全绿，含 01–15 旧基线回归）/ `pnpm build`；16–20 全部 solver 可解、最短解与断言一致、trace 回放确认连锁/奇偶/断链按设计发生。
- 遗留问题：
  - 21–30 段候选（anti-periplanar 消除 / 弹射落点）未动；「消去」需要空臂概念，规则重量待评估。
  - 共振连锁的动画阶梯只按中心距离近似传播顺序（渲染层未拿引擎的真实传播序）——观感正确、语义近似，暂不修。
  - 白圈修正后需要再找试玩确认箭头/✓/共轭键的可读性。
  - 「第 5 关测试」复测结论仍待人工试玩：共振是否让《109.5°》追平 t3 的「每关一个新理解」。

## #5 2026-08-24 — 《109.5°》v1 基团搬运规则 + 11–15 复测批次 + 渲染手感

- 改了什么：
  - **引擎 v1（design §5 正式规则落地，v0 简化声明预留的方向）**：关卡可声明游离色珠 `groups: [{pos,color}]`；走上色珠格拾取、持物再走上则交换（动作空间仍为四方向）；空手进攻 = v0 纯翻转（旧关完全兼容）；持色珠进攻 = 取代 + 翻转（色珠装到正对攻击者的臂、开口臂旧色换入手中）。目标校验放宽为「颜色存在于本关任一臂或色珠」。`stateKey` 纳入手持与场珠。
  - **关卡 11–15 入库**（v1 复测批次，教学序列见 design §5 批次表）：11 拾取投放（6 步）/ 12 跨中心取送（11）/ 13 循环交换（17）/ 14 两种目标·空手一击时机（18）/ 15 毕业题·共享站位（16）。全部经 `pnpm solve` 验证，最短解断言进 `levels.test.ts`（批次 01–15，47 项）。
  - **渲染手感三件套**（`render.ts` 重写 + `shell/main.ts` 加渲染循环）：行走补间与翻转 180° 旋转动画（`core/tween.ts` 首次接入；动画状态是渲染层私有时钟，按 stateKey 转移驱动，大跳/换关自动重置）；无效进攻抖动 + 撞面红闪（`Bundle.onBlocked` 钩子）；已达标中心锁定圈（「别碰已达标」一眼可见）；另画游离色珠与手持色珠、HUD 加「手持」读数、背景四面体缓慢自转（shell 现在常驻 rAF 循环）。
  - 新工具 `scripts/trace-chem.ts`（`pnpm trace:chem <level-XX>`）：逐帧回放最短解（玩家/手持/各中心臂与开口/达标状态），「预期解 vs 实际最短解」比对正式工具化。
- 为什么：01–10 试玩反馈确认 v0「进攻 = 纯翻转」的奇偶上限是无聊根源（每中心仅 2 种构型，谜题退化为路线 + 拨开关）。v1 把进攻从「拨开关」升级为「搬运 + 交换」：轴臂成为可注入颜色的插槽，构型空间爆炸，移动有了取货—送货—换货的目的性；11–15 即「第 5 关测试」的复测材料。
- 制关中推导并验证的两条新结构不变式（已写入 design §5 推论 4/5）：
  - **手持不变式**：手持一旦非空永不为空（拾取/交换/进攻都只替换手中颜色）⇒ 空手翻转的机会只在开局，空手谜题是开局顺序谜题。level-14 就是按此构造：P 双目标（armE=blue + armW=red）只有空手一击能同时达成，任何携带物都会砸坏 armW。
  - **提取槽**：每次进攻的提取槽 = 开口方向那条臂，且开口只在两轴臂间交替 ⇒ 垂臂内容永不可提取、起始在非开口侧轴臂上的颜色也永不可提取（每次进攻都被甩回非开口侧）；每个中心「初始可提取」的颜色至多一种。
- 「预期机制 vs 实际最短解」留档（两次返工，pipeline 证据）：
  - **level-15 返工**：初版 C2 目标 armE=yellow、意图「空手翻转转开口 → 再投放」。solver 最短解（27 步）把「别碰」的 C1 当**颜色提款机**：先持黄撞 C1 换出红、空手期用红撞 C2、再持红撞 C1 复原并取回黄、最后投黄——即「先空手翻转」路线根本不存在（持黄撞 C2 会把黄埋进 W 臂成死循环）。返工：C2 改为一击必中的翻转目标（armE ← 旧 armW，与手持无关），毕业题变为 拾蓝 → 共享站位向南投放/换黄 → 顺路一击，16 步，C1 全程不碰。
  - **level-14 返工**：初版意图「空手撞 P」但 P 单目标 armE=blue——回放发现 armE ← 旧 armW 与手持无关，solver 直接持珠撞 P 也达标，「空手」教学是假的。返工：P 加第二目标 armW=red，携带物会砸坏它 ⇒ 空手一击被手持不变式强制为开局第一动（先空手翻转 → 再拾珠 → 取蓝 → 送蓝，18 步，顺序完全唯一）。
- 如何验证：`pnpm typecheck` / `pnpm test`（94 项，含 v1 引擎语义、01–15 基线、渲染冒烟覆盖动画转移与撞击反馈）/ `pnpm build`；11–15 全部 `pnpm solve` 可解且最短解与断言一致；trace 回放逐帧核对与设计意图一致。
- 遗留问题：
  - v1 规则深度只验证到 5 关；16–20 段（搬运 × 路线 × 多中心组合）与 21–30 段（anti-periplanar 消除 / 弹射变体）待定题后推进。
  - solver 在 v1 下状态数上升（level-15 约 2.4k 访问，仍远低于上限）；后续大关注意 `maxVisits`。
  - t3 渲染仍未接动画循环（shell rAF 已就绪，`setDecor`/`onBlocked` 钩子 t3 未实现，为无操作）。
  - 「第 5 关测试」复测结论待人工试玩：v1 后《109.5°》的组合空间是否追平 t3 的「每关一个新理解」。

## #4 2026-08-23 — 《109.5°》主题化渲染落地（design §10 化学味方向）

- 改了什么：
  - 重写 `src/games/chem/render.ts`：中心改为分子骨架风格（细键线 + 臂原子 + 中心原子点）；背景加极低透明度（α≤0.06）四面体线框（键角 109.5° 的几何本体，顶点即原子点）；底部 HUD 仪器化（刻度尺 + 步数三位读数 + 开口读数）；棋盘四角加仪器角框。
  - 装饰开关（§10 纪律「可用一个开关整体关掉」）：`setChemDecor()` + shell 头部「✦ 装饰 开/关」按钮；`Bundle.setDecor` 设为可选协议，当前仅 chem 接入。关掉只去除装饰，开口圈 / 箭头 / 目标虚线等玩法信息永远保留。
  - 纪律落实：布局常量不变（装饰不压缩棋盘）；渲染内文案无任何化学术语。
  - 新增渲染冒烟测试 `render.test.ts`（Proxy 桩画布直通单/多中心关卡 + 装饰开关两种状态）。
- 为什么：落地 design §10 轻包装方向；《109.5°》的化学味是答辩揭晓记忆点的一半，标题几何（四面体）先进背景。
- 如何验证：`pnpm typecheck` / `pnpm test`（68 通过，含渲染冒烟）/ `pnpm build`。
- 遗留问题：
  - 《t+3》的科幻/时间感渲染与两游戏标题画面（大字 + 程序化动效）尚未实现。
  - 装饰按钮当前只对《109.5°》生效（t3 未实现 `setDecor` 前为无操作）。
  - 渲染为静态帧（无 rAF 循环），背景四面体不旋转；若后续要动效需先给 shell 加渲染循环。

## 前史（代码前阶段）

策划讨论阶段的候选方案评估与淘汰记录（Echo Grid 因撞车「黄金替罪羊」被毙、t+3 转向、化学方向从「知识游戏」转为「原理即规则」）已整理为结论，见 `docs/design.md` §2 与 §5。原始讨论稿为本地临时文件，不入库。
