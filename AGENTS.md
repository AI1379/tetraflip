# AGENTS.md — 仓库协作规范

本文件约束所有在本仓库工作的 AI 编码代理与人类协作者。**改动任何代码前先读完本文件。**

## 项目一句话

比赛 H5 解谜游戏（≥50 关、纯玩法）。已正式定题 `src/games/chem`（《109.5°》，SN2 立体化学抽象）作为唯一提交作品；
`src/games/t3`（《t+3》，延迟多层控制）只保留为落选研发档，不进入正常提交入口。
机制定义、命名与视觉包装方向见 `docs/design.md`，时间线见 `docs/iteration.md`。

## 硬约束（比赛规则）

- 纯 H5 静态页，无后端、无联网运行时依赖
- 关卡 ≥50 个，全部 JSON 化
- 不做故事；美术包装非比赛要求，本项目只做零美术素材的轻包装（方向见 `docs/design.md` §10），评审重心仍是玩法
- 截止 2026-08-31，时间极紧：一切决策优先权衡「实现风险 × 好玩收益」，避免过度工程

## 技术栈铁律

- vanilla TypeScript + Vite + Canvas 2D；唯一允许的运行时依赖是 `zod`
- 新增任何依赖必须有充分理由，默认拒绝
- 禁止引入前端框架、状态管理库、物理引擎、构建期之外的运行时服务

## 架构

- `src/core/` 共享层：`protocol.ts`（GameDefinition 协议）、`solver.ts`（通用 BFS）、`undo.ts`、`levels.ts`、`keyboard.ts`、`tween.ts`
- `src/games/<id>/` 每个游戏一个目录：`level.ts`（类型 + zod 校验）、`engine.ts`（纯函数引擎）、`render.ts`、`levels/*.json`
- `src/shell/` 浏览器胶合层（DOM / 键盘 / HUD / 画布宿主）——**只有这一层允许 `any`**（因为要桥接异构游戏类型）
- 引擎层与 core 层：零 DOM、零副作用、纯函数，必须能在 Node 中被 import（solver、测试、`scripts/solve.ts` 都依赖这一点）
- 所有状态转移走 `step(state, action) → 新 state`（不可变）；渲染层只读状态，禁止改状态

## 关卡规范

- 文件名 `level-XX.json`，序号零填充（决定关卡顺序）
- 入库关卡必须通过：zod 校验 + `pnpm solve <game> level-XX` 可解
- 关卡的最短解长度应符合该关的设计意图（不要出现绕开核心机制的捷径；发现捷径要改图并在文档中记录）

## 文档要求（比赛硬性要求，未遵守视为任务未完成）

比赛要求提交**详细的设计思路文档**与**迭代演进文档**，对应仓库中：

1. `docs/design.md` — 设计思路：机制定义、候选方案对比与淘汰理由、50 关曲线、AI 关卡 pipeline、架构决策。
   **任何玩法 / 规则 / 架构层面的决策变更，必须先在此文档记录「决策 + 理由」，再动代码。**
2. `docs/iteration.md` — 迭代演进：按时间追加条目。每个有意义的迭代（新机制、规则修改、关卡批次、pipeline 进展、重要修复）必须记录：日期 / 改了什么 / 为什么 / 如何验证 / 遗留问题。
3. 提交自检：涉及玩法、规则、关卡、架构的改动 → 两份文档同步更新；纯渲染 / 测试 / 重构 → 至少在 `iteration.md` 记一行。

## 其他纪律

- `prompt.txt` 是外部策划草稿，已通过 `.git/info/exclude` 排除，**不要提交进仓库**；也不要把外部对话原文复制进仓库——文档只写提炼后的结论与决策
- 引擎逻辑改动必须附带 vitest 测试（`src/**/*.test.ts`）
- 提交前跑：`pnpm typecheck && pnpm test && pnpm build`
- 提交信息用简洁中文或英文均可，说清「改了什么 + 为什么」

## 当前开发 TODO — 《109.5°》v3.2 定题收敛（2026-08-24）

> **2026-08-28 v5 更新**：下列历史 TODO 中的关卡编号在 v5 曲线重构后已整体漂移（决策与完整映射见 `docs/design.md` §5「v5 曲线重构」、时间线见 `docs/iteration.md` #54）。现行曲线为：01–06 核心搬运；07–12 共振（07 首现即教学、09《碰不到的中心》为新关）；13–17 光照分步；18–23 空穴；24–29 弹射；30–36 护罩；37–43 结构碰撞（43 为提升的候选「一条臂，两扇门」）；44–50 综合 mastery（46/48/49 为提升候选，50《终局：三个阶段的闭环》恢复唯一终局命名）；51–56 赛后扩展池。删关：旧 08/09/10/15/40。真人盲测（P2 视觉验收、P3 第 5 关门槛、P5–P7 各盲测项）仍全部未完成，是提交前的首要验收缺口。
> **2026-08-28 v5.1 更新**：新手教程边界收紧为“只讲新规则的输入与结算，不给当前关颜色路线 / 先后顺序 / 唯一洞见”。05 起独立解题；06 改名《先碰哪一座》并撤掉空手不变式教学；07 共振压为两拍规则说明；09 撤掉“唯一的门”自动揭题。地图、par 与编号不变，见 `docs/design.md` §5 v5.1。
> **2026-08-28 v6 更新**：高难扩展两批入库——57–66 全机制组合池、67–74「转辙与红线」池（引擎新增可选 `moveLimit` 步数预算字段，语义见 `docs/design.md` §5 v6；57–66 均带红线 par+3）。两批难度排序与删留待真人试玩终审；物理序号 75 为独立 LV.999 彩蛋。

本节是下一轮实现的**有序清单**，规则理由与精确定义见 `docs/design.md` §5「v3.2 定题收敛与空穴深化」，决策留档见 `docs/iteration.md` #15。当前仅完成设计冻结，下面未勾选项均未宣称已实现。除非新试玩证据推翻，不重新引入已删除的外围功能砖。

### P0 — 规则瘦身与数据迁移（同一批完成，避免半兼容状态）

- [x] 从 chem 关卡 schema / state / `stateKey` / engine / render 删除 `disposals`（回收格）、`launchers`（弹射台）、`deprotections`（独立脱保护格）与全局 `deprotected` 状态；不要保留无关的向后兼容死字段。
- [x] 删除对应射线发射、主动销毁、踩锁解全场保护的引擎分支、渲染图形与旧测试；动作空间继续只有四方向移动。
- [x] 弹射中心 `ejects` 保留：持珠取代后手变空，被顶出珠沿攻击反方向落到射线最后空格；它是后期唯一主动恢复空手的规则。
- [x] 把旧 `shielded: true` 迁移为阶段字段（暂定 `shieldUntilStage: number`）：`stage < shieldUntilStage` 时阻挡进攻与共振；进入指定阶段后永久解除。
- [x] 阶段护罩只在当前动作的取代 / 翻转 / 共振全部结算后解除，刚结束的连锁不得追溯穿透；下一动作起才视为连通。
- [x] 光照仍可移动罩内中心的开口；护罩只挡直接进攻与共振，不冻结全局开口转轴。
- [x] 为以上规则补 vitest：阶段前阻挡、阶段后放行、完成阶段当步不追溯、下一步可传播、光照穿透护罩、多护罩按不同阶段依次解除、`stateKey` 不再含旧全局状态。

### P1 — 三臂「空穴」深化（不新增操作语法）

- [x] 固定空穴边界：空穴无颜色、不可拾取 / 注入 / 填补，开口不能停在空穴；光照跳过空穴但不移动它；中心始终恰好三条真实臂。
- [x] 固定共振边界：相邻双方必须都有面对臂且同色才能共振，任一侧为空穴即无键；不得把两个 `undefined` 当作同色连接。
- [x] 固定时序：三臂中心被直接进攻或共振命中后，三臂与空穴先整体翻转到对侧，再按翻转后的真实臂继续检查传播；两次翻转恢复原拓扑。
- [x] 目标不得把「空穴本身」当颜色；允许目标指向初始为空穴的槽位，但必须靠后续翻转让真实色臂进入该方向。
- [x] 至少制作四类语义约束关：① 空穴断开危险链；② 翻空穴保护已达标链尾；③ N/S 或 E/W 双路换向；④ 第一阶段断链、阶段护罩解除后翻空穴接通第二阶段。
- [x] 空穴关的测试不能只断言最短步数；必须回放最短解或做约束搜索，证明目标共振确实因空穴移动而接通 / 断开，没有等长捷径绕开。
- [x] 组合优先级：先做「空穴 × 共振」，再做「空穴 × 光照」，最后做「空穴 × 阶段护罩」；弹射组合只在前三者验证有趣后加入，避免一次堆四条规则。

### P2 — 弹射中心与阶段护罩的视觉可读性

- [x] 将 `ejects` 明确传入中心绘制函数；弹射中心必须有不同于普通圆核 / 三臂三角核的静态轮廓，不能只靠 hint 或执行后动画辨认。
- [x] 在弹射中心的出射侧常驻短尾迹 / 双箭头，随开口轴变化；玩家持珠站到合法进攻位时，预演完整弹道、被顶出珠颜色与最终落点。
- [x] 执行动画显示珠子沿预演路径飞出，手持同步清空；若身后第一格被堵导致进攻无效，给喷口受阻反馈而非普通撞面反馈。
- [x] 阶段护罩使用强轮廓 + 对应阶段编号 / 颜色；未来段目标与护罩使用同一编码。阶段完成时播放破裂 / 消散反馈。
- [x] 按住预演若会完成当前阶段，应预览护罩解除，但不得伪造同回合追溯共振。
- [ ] 视觉验收：不看 hint，试玩者应能在 1 秒内指出弹射中心；看到护罩编号后能说出它会在哪一阶段解除。

### P3 — 正式 01–10 重排（第三关即进入取代）

- [x] 01：两拍对象绑定教学——先滑动到固定进攻位，再聚光四元中心并完成 180° 翻转。
- [x] 02：从错误侧移动到背面，只教读取开口；棋盘紧凑，不做迷宫。
- [x] 03：拾珠并第一次持珠取代，明确「手中珠进入中心」。
- [x] 04：聚焦「原开口臂色珠换到手中」，让输入 / 输出在两步内得到反馈。
- [x] 05：从 A 取货送到 B，第一次跨中心物流。
- [x] 06：连续交换，发现持珠后手不会自然变空。
- [x] 07：同关对比轴臂插槽与垂臂奇偶锁。
- [x] 08：空手只有开局一次，必须先纯翻转再拾珠。
- [x] 09：共享站位 + 克制，不碰已达标中心。
- [x] 10：紧凑三中心物流毕业，不新增规则，不用房间迷宫拉长。
- [x] 优先复用并前移现 11–15 的成熟颜色物流；旧 03–10 的 v0 路线关允许退休，不因沉没成本保留。
- [x] 01–02 之外，目标决策密度原则上 ≥35%、最长连续纯移动原则上 ≤3；墙只能制造有意义的进攻站位 / 资源约束，不能只增加通勤。
- [ ] 盲测门槛：玩家在第 5 关后能主动说明「我会放入什么、换出什么」，否则先修教学与反馈，不继续加关。

### P4 — 21–40 旧批次迁移与 44 关新曲线

- [x] 旧 22 / 26（回收）改作光照 / 分步组合题；旧 32（弹射台教学）退休。
- [x] 旧 33 / 36 改为阶段护罩题；不得保留「走到角落踩锁」路线。
- [x] 旧 35 改为三臂弹射中心，用缺口 / 障碍控制落点；旧 39 改为光照改变弹射中心进攻轴与出射方向；旧 40 改为阶段护罩 × 空穴 × 光照 × 弹射毕业题。
- [x] 保留并复核旧 21 / 23–25 / 27–31 / 34 / 37 / 38；其中 31 的最长连续通勤为 6，须先压缩路线再入新曲线。
- [x] 正式定题《109.5°》；曲线重排为 01–09 核心搬运；10–15 共振；16–20 光照与分步目标；21–26 三臂 / 空穴；27–32 弹射中心；33–39 阶段护罩；40 复习过渡；41–46 弹射撞结构 / 护罩再生；47–50 综合 mastery，50 为唯一终局。
- [x] 第二轮瘦身：删除与已有关卡重复的 7 / 16 / 21 / 43 / 45 / 47，正式曲线先收敛为 44 关；随后补六张 v4 关恢复到 50（初始旧编号 45–50，定题后已重排至 41–46）。

### P6 — v4 新机制（41–46 已进正式曲线，47–50 收束 mastery）

- [x] 弹射打结构联动：`ejects` + 可选 `hitLights` / `hitCenters`；弹射珠落光照格触发转轴、落中心进攻位触发纯翻转 + 共振传播；zod 校验 + 引擎测试。
- [x] 护罩再生：`reactiveTo` 中间产物被破坏时护罩回来、修复后重新打开；可与 `shieldUntilStage` 叠加；引擎测试。
- [x] 用新机制制作六关并排入 41–46；41 弹射撞核、42 撞光又撞核、43 关闸保形（关盾挡共振）、44 回授闸门（闭→开→闭→开）、45 护罩缓冲（关盾挡飞珠撞核）、46 撞核共振链；47–50 回到既有规则综合并由三阶段闭环收尾。
- [ ] 真人盲测这两个机制：能否预测弹射触发 / 再生护罩的开合。

### P7 — 51–60 综合候选池（先扩量，再删选）

- [x] 不新增 schema / 操作语法，新增 10 张至少组合三类既有机制的候选关；01–50 仍视为正式曲线，51–60 可直接试玩但不预设全部保留。
- [x] 逐关建立 BFS 最短解与 JSON `par` 基线；用语义轨迹证明弹射撞核、缺口开链、互补闸门、双喷口接力、余料终投等核心关系不可被等长绕开。
- [x] 选关面板增加 51–60「综合候选池」章节；50 去掉终局命名，60 暂作候选终局。
- [ ] 按 `docs/playtest.md` 做成对真人盲测，记录预测错误、撤销 / 提示、首通时间与重玩偏好；不要用开发者自测代替。
- [ ] 根据语义重复、走路税与盲测结果删关；删选后统一重编号、更新最短解基线，并恢复唯一终局命名。

### P5 — 每批提交前验收

- [x] 玩法 / 规则 / 关卡改动先同步 `docs/design.md`，完成后追加 `docs/iteration.md`；不要把本 TODO 的「待实现」误写成已完成。
- [x] 所有关卡通过 zod、`pnpm solve chem level-XX` 与最短解基线；涉及空穴 / 护罩 / 弹射的关卡另加机制语义轨迹或约束搜索。
- [x] 跑 `pnpm audit:chem` 检查决策密度与最长通勤；指标是报警器，保留长路必须能说明它承担的站位或风险意义。
- [ ] 每个新机制段至少找一名未接触该规则的玩家盲测；记录首次误解、是否能预测下一步和卡关是否来自推理而非记忆。
- [x] 最终执行 `pnpm typecheck && pnpm test && pnpm build`，并确认正式关卡不少于 50 个、JSON 全部入库、无旧字段残留。

## 常用命令

| 命令 | 用途 |
| --- | --- |
| `pnpm dev` | 本地开发服务器 |
| `pnpm test` | 运行测试（vitest） |
| `pnpm typecheck` | 类型检查（tsc --noEmit） |
| `pnpm build` | 产出静态站点（dist/） |
| `pnpm solve <t3\|chem> level-XX` | 命令行 solver 验证关卡可解并打印最短解 |
